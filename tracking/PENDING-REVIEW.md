# HÀNG ĐỢI CHỜ CƯỜNG CHECK

Cường (2026-07-26): *"note lại các phần cần tôi check, sẽ check sau, **nhắc lại sau mỗi lần update**"*.

**Quy tắc cho agent:** mỗi lần báo cáo xong một UPDATE, **luôn nhắc lại danh sách mục `⏳ CHỜ` dưới
đây** (ngắn gọn: mã, cái gì, xem ở đâu). Không được im lặng bỏ qua chỉ vì đang bận việc khác.
Khi Cường cho verdict → chuyển sang phần "Đã xong", ghi ngày + kết luận.

> Cường đã **hoãn** (không phải waive) visual gate của SIM-1/SIM-2 để agent chạy tiếp. Vì vậy các
> mục dưới đây **vẫn còn hiệu lực**, chỉ là không chặn commit.

## ⏳ CHỜ CHECK

| Mã | Cần check gì | Xem ở đâu | Từ ngày |
|---|---|---|---|
| **V-01** | **SIM-1 realism** — mật độ tài xế lúc 05-07h (trước đây trống trơn), đuôi đêm 21-23h, cuốc bị huỷ giữa đường đi đón | Dashboard tab 🗺️ Bản đồ H3 + 📈 Theo thời gian, **seed 1000** | 2026-07-25 (UPDATE-044) |
| **V-02** | **Fix visual trạm sạc bị cột H3 đè** — bật/tắt checkbox *"Xem phẳng (2D)"*, kiểm chấm xanh viền trắng có nổi rõ ở cả 2 chế độ không | Dashboard tab 🗺️ Bản đồ H3 | 2026-07-25 (UPDATE-044) |
| **V-03** | **SIM-2 hành trình tài xế** — Gantt timeline, bảng offer có LÝ DO, và đường thu nhập **không có bậc thưởng** ở tân binh (đó là `D-SIM-02`) | Dashboard tab 🧭 Hành trình 1 tài xế → **seed 1000 → `d-30 · P4 TÂN BINH`** | 2026-07-25 (UPDATE-045) |
| **V-04** | **SIM-3 cầu nối advice→action** — bật `advice.enabled=true` + `single_actor_id` = tài xế P4, xem các mốc advice trên timeline và cột theo/không-theo | Dashboard tab 🧭 | 2026-07-26 (UPDATE-046) |
| **V-05** | **SIM-4 thế giới song song** — chạy `uv run python scripts/run_parallel.py --seeds 30`, đọc bảng Δ + CI + `n_pos`. Tab A/B đã được bổ sung ở SIM-XANH P4 (xem thêm V-09), nhưng kết luận 30 seed vẫn đọc từ canonical evaluator/CLI | CLI + tab Thế giới song song; UPDATE-047/057 | 2026-07-26 |
| **V-06** | **SIM-5 data mới + bộ metric** — kiểm bộ 90 ngày/`sim_metrics.full_report` (chờ khách, mật độ hex×giờ). **Đính chính hiện trạng:** tab A/B nay đã có theo V-09; snapshot 90 ngày vẫn là MOCK và R2 chưa artefact-verify trực tiếp | Dashboard + `scripts/regen_mock.py` + dossier `01-*` | 2026-07-26 |
| **V-07** | **D-SIM-03 kênh `rest_window`** — câu hỏi “có ưu tiên sim nhiều ngày không” đã bị vượt qua vì `D-SIM-10/13` đã implement. Phần Cường còn cần check là semantics/event `rest_window` sau multi-day có hợp lý hay vẫn inert/sai cadence | UPDATE-050/052/053 + ĐA-04 | 2026-07-26 |
| **V-08** | **Sim nhiều ngày + data liên tục** — (a) bảng ngày-qua-ngày UPDATE-052 §4 L1: biến động **5→18 cuốc/ngày** của cùng tài xế có hợp lý không? (b) UPDATE-053 §2: **autocorr ngày-qua-ngày ≈ 0** (chưa có persistence hành vi — ngày nghỉ/thói quen/ốm) có chấp nhận được cho bản publish mock, hay cần `D-SIM-16` trước? | UPDATE-052 §4 + UPDATE-053 §2 | 2026-07-26 |
| **V-09** | **SIM-XANH dashboard mới** — thứ tự xem: tab **Replay** (kéo slider quanh 07:00/18:00), tab **Hành trình** (d-41 P4: Gantt palette mới + vạch advice + sao/mission/tân binh), tab **Thế giới song song** (bấm Chạy cặp A/B; heatmap độ nhạy nếu sweep xong) | `uv run --extra viz streamlit run src/gsm_sim/dashboard.py` (UPDATE-057 §7) | 2026-07-26 |
| **V-10** | **Track UI — web app thật trên nền Khánh** (UPDATE-059..063). Thứ tự xem: (1) **App tài xế** `http://localhost:8010/app/` — Xanh Now (payout từ snapshot **MOCK L1R-shaped** d-19 · demand hex cyan · badge mock), bấm 🤖 **Trợ Lý Xanh** đổi 3 khung giờ (9h feasible / 19h nói thật không khả thi / 21h30 im lặng), màn **Thu nhập** (payout mặc định · gross tách · est_net "—"), **Cài đặt** đổi hồ sơ (thử `cp-0` → bot im lặng đúng "chưa phủ đội car"); (2) **Khu Mô phỏng** `/app/mo-phong/` — Replay bấm ▶ quanh 07:00/18:00, Hành trình (Gantt + income 4 nguồn + marker sự kiện + bảng offer), A/B bấm chạy cặp (~40s, đọc warning 1-seed), Độ nhạy (heatmap ✳). Server: `uv run uvicorn app.main:app --app-dir ui/backend --port 8010` | UPDATE-063 §Kịch bản | 2026-07-26 |
| **V-11** | **Data semantics hiện tại** — app mặc định ngày cuối snapshot 2026-09-28 (MOCK tương lai); bấm cuốc interaction mẫu rồi reload state để thấy payout/acceptance/completion **không đổi**. Xác nhận đây là gap ingestion/projection, không phải Advisor được phép ghi data | Dossier `2026-07-27-current-state/01-*` + `/api/v1/driver/default-view`/`state` | 2026-07-27 |
| **V-15** | **BUG-S2-PARAMS + lịch khuyên đổi 72%** (UPDATE-078) — advisor nay khuyên NGHỈ nhiều hơn hẳn (`OOO` → `OOR`); nghỉ bắt buộc 30′ → 120′ cho ca 10h. **Δ payout xấu đi −17.310đ → −24.960đ**, và hồ sơ `11-*` giải thích vì sao đó là **lỗi của thước đo, không phải của lời khuyên**. Xem: khu Mô phỏng → Hành trình seed 1000 (Gantt + vạch advice), rồi tab Thế giới song song. **Kèm câu hỏi cần Cường quyết: có đồng ý ưu tiên thêm hậu quả của MỆT vào sim (T-041 bước 1c) trước khi đụng số hạng chi phí của solver không?** | UPDATE-078 + hồ sơ `10-*`, `11-*` | 2026-07-27 |
| **V-14** | **ĐA-01 gỡ rò tương lai** (UPDATE-077) — **16/60 tài xế đổi phía ngưỡng 0,85** ⇒ kết luận gate thưởng đổi. **Có ca thật để xem**: `d-37` @ `2026-09-28` 09:00 (tỷ lệ *cùng ngày* 1,000 nhưng lịch sử 7 ngày 0,745 ⇒ **nay CÓ cảnh báo**); đối chứng ngược `d-2` (0,636 → 0,881 ⇒ **nay THÔI cảnh báo**). Xem cả hai để thấy thay đổi không một chiều | UPDATE-077 §Visual | 2026-07-27 |
| **V-13** | **Card cảnh báo "đủ điểm nhưng thưởng sắp mất"** (UPDATE-076) — trạng thái trước đây advisor IM LẶNG nay hiện cảnh báo. **Không dựng được ca thật**: quét 12.805 driver-day → 0/86 ca kịch mốc có tỷ lệ dưới ngưỡng (biên hụt đúng 10 điểm / 0,007 tỷ lệ). Cần Cường chọn: (a) chấp nhận bằng chứng test + bảng quét, hay (b) dựng fixture demo có nhãn MOCK để xem tận mắt | UPDATE-076 §Visual + §Adversarial | 2026-07-27 |
| **V-12** | **Hai demo đúng vai trò** — Khu Mô phỏng dùng cho dispatcher/researcher chạy và visualize hiệu quả toàn hệ thống; app demo + Advisor chỉ là một tài xế. Không yêu cầu UI giống nhau; yêu cầu cùng run/actor/as-of reconcile data | Dossier `2026-07-27-current-state/02-*` | 2026-07-27 |
| **V-16** | **Fare parity gate** — tại `/app/` chạy incoming → active → completed; đối chiếu route API và UI `gross=19.450đ`, `payout=14.588đ`, `sim-policy-v0 · MOCK` cho fixture 3,5km; kiểm tra income pill/chart/ledger không đổi. Mở `/app/mo-phong/` và đối chiếu offer gross với engine `PolicyBundle`. ⚠ **Đánh số lại V-11 → V-16 khi merge 2026-07-28**: hai nhánh cùng dùng mã V-11 cho hai việc khác nhau (nhánh này: data semantics). Không bỏ mục nào. | UPDATE-073 §Visual verification | 2026-07-27 |

| **V-17** | **Kênh VỊ TRÍ (b3/b4, UPDATE-083/084)** — Replay seed 1000 với `advice.positioning_overrides: wait_only`: xem event `standby_alloc`/`standby_followed` trên timeline + tab Bản đồ H3 (HHI cung GIẢM có ý nghĩa — phân bố cung phải NHÌN thấy trải đều hơn); đối chiếu điểm TRẢ khách nay bám khu đông (α=0.4) | UPDATE-083 §Visual + UPDATE-084 | 2026-07-28 |

## ❓ QUYẾT ĐỊNH CẦN CƯỜNG CHỐT (không phải visual)

| Mã | Cần chốt gì | Vì sao cần Cường | Từ ngày |
|---|---|---|---|
| **Q-11** | ⭐⭐ **BUG-EVAL-ARGMAX (UPDATE-085 §4): duyệt đổi tiêu chí 1 của ĐA-08.** `pick_target` chọn tài xế MAX-offers của thế giới A = chọn CỰC TRỊ ⇒ Δ(B−A) trên người đó **âm bất kể nội dung can thiệp** (regression to the mean). Chứng minh sign-flip 5 seed: argmax-A **−19,7k** · argmax-B **+27,4k** · không-chọn-lọc mean-P4 **+3,6k** · toàn đội +5,4k/người. ⇒ Chuỗi "advisor làm tài xế nghèo đi −17k…−40k" (UPDATE-075/078/081/084) là artifact đo; **các tầng hệ thống không bị ảnh hưởng** (aggregate không chọn lọc — served/HHI/Gini/tổng payout giữ nguyên). **Đề nghị**: tiêu chí 1 = mean Δpayout trên MỌI tài xế được phủ (tách theo archetype), ≥30 seed; giữ argmax làm view chẩn đoán có nhãn bias. ⚠ +3,6k mới là 5 seed — CHƯA được tuyên chiều ngược lại. | Đổi định nghĩa tiêu chí nghiệm thu ĐA-08 = quyết định sản phẩm; và đây là cơ sở đọc lại toàn bộ số cũ | 2026-07-28 |
| ~~Q-01~~ | ✅ **RESOLVED-BY-FETCH 2026-07-26** (Cường cho phép tự fetch): cấu trúc thật từ greensm.com — bike: combo 810k (clawback nếu <200 cuốc/tháng×2 tháng), mốc ≥50 cuốc/7 ngày đầu, bảo lãnh doanh thu 90 ngày (số image-locked → PROXY); taxi 3M/90 ngày làm tham chiếu. Mô hình hoá ở Phase 2 SIM-XANH với nhãn nguồn + confidence; SỐ THẬT vẫn chờ GSM (D-POL-05) | — | đóng 2026-07-26 |
| ~~Q-02~~ | ✅ **CLOSED 2026-07-26** (Cường hỏi free alternatives): KHÔNG cần Google key — OSRM (routing, không key, đã test) + Stadia (tiles/geocode) + OSM data (geometry/trạm đã dùng từ đầu). OSRM đưa vào SIM ở Phase 1 SIM-XANH | — | đóng 2026-07-26 |
| **Q-07** | ➡ **Cường chọn (c) 2026-07-28**, kèm chỉ thị: *"phải nghiên cứu kỹ cách H3 và hệ thống dispatch đang vận hành và giả lập thực tế nhất. **không ưu tiên dễ, nhẹ mà ưu tiên thực tế**"*. ⇒ **KHÔNG** làm bản (c) rẻ mà agent đề xuất (chỉ sửa phép đo của test). Phải nghiên cứu dispatch thật + cách dùng H3 trong production rồi thiết kế lại cho đúng thực tế. Đang làm — xem hồ sơ `15-*`. Chi tiết đánh đổi đã đo: `configs/pilot_dongda.yaml:93-115` | Hồ sơ `14-*` §2 | 2026-07-28 |
| ~~Q-07 (nội dung cũ)~~ | ⛔ **ĐÁNH ĐỔI KHÔNG THỂ TỰ QUYẾT: ghép đơn đúng vs trung thành archetype.** `candidate_ring_k_max = 6` phủ **2,22 km** trong khi `eta_max = 11′` cho phép tới **3,14 km** ⇒ tài xế thoả ETA ở 2,2–3,1 km bị loại **âm thầm** ⇒ đơn chết dù có người trong tầm. **Nhưng nới k thì tỷ lệ nhận thực tế trôi khỏi `accept_base` quá dung sai 5pp** (k6 −0,042 ✅ · k7 −0,053 ❌ · k8 −0,057 ❌), vì đơn mới phục vụ chính là đơn **pickup xa** — tài xế từ chối nhiều hơn là **hành vi đúng**. Đổi lại: served 0,761→0,788, hết hạn 233→196, 18h hết hạn 38,8%→34,5%. Agent **đã hoàn tác về k=6** để không để test đỏ và **không nới dung sai** (đó là che khuyết tật). Cần Cường chọn: **(a)** giữ k=6, chấp nhận khuyết tật ghép đơn · **(b)** nới k=8, chấp nhận archetype kém trung thành hơn + sửa dung sai test **có lập luận** · **(c)** sửa gốc: cho `decide_accept`/`accept_base` tách bạch khỏi số hạng kinh tế của pickup xa | Hồ sơ `14-*` §2 + `configs/pilot_dongda.yaml:93-115` + `tests/test_dispatch_shortlist_radius.py` | 2026-07-28 |
| **Q-03** | **`D-POL-04`: corpus của Khánh** thiếu policy Vận Doanh 23/02/2026 (bỏ phạt ≤70% + khoán tuần) → F0 có thể trích dẫn policy CŨ | File thuộc claim của Khánh, agent không tự sửa | 2026-07-24 |
| **Q-04** | Duyệt UX proposal: dismiss/no-response/behavior outcome tách rõ; rest advice không lặp trong cùng window/shift; personal goal ≠ policy quota ≠ mission; recap fancy nhưng không coercive | Đây là product behavior mới được nghiên cứu chi tiết, chưa có approval implementation | 2026-07-27; dossier `03-*` |

> **Ghi chú phạm vi "duyệt hết" (2026-07-27):** Cường duyệt **spec objective v2 + ĐA-04..ĐA-09**
> (xem bảng ✅ dưới). **Q-04 và Q-03 KHÔNG nằm trong lần duyệt đó** — Q-04 chưa được nêu lại trong
> hội thoại duyệt, Q-03 thuộc claim của Khánh. Cả hai vẫn ⏳.

## ⛔ BLOCKER KỸ THUẬT KHÔNG ĐƯỢC QUÊN

| Mã | Blocker | Cần làm |
|---|---|---|
| ~~B-01 / R5-MUT10~~ | ✅ **GỠ THẬT — đã commit `6ccd8fc` (2026-07-28)**, kiểm lại theo yêu cầu Cường. Bằng chứng: (1) code hiện tại `return max(1, round(soc_cost_per_bucket * bucket_min / 30.0))`; (2) **re-apply mutation → `test_soc_cost_scales_with_bucket_min` ĐỎ** (`assert 1 == 2`) → restore → **xanh**; (3) doc `UPDATE-074` đã trong lịch sử git. **Đính chính lần trước:** mutation **chưa bao giờ lên remote** — `7739b3c` là commit local chưa push, `origin/main` luôn sạch; nói "HEAD còn mutation" chỉ đúng với HEAD **local**. ⚠ **Một nửa lan can yếu**: trong 2 test thì `test_soc_budget_binds_at_60min_buckets` **vẫn XANH khi có mutation** (dùng `>=` nên không giết được mutant) — chỉ test unit thật sự canh. Đã ghi vào T-046. | — |
| **B-02 / ARCH-VERSION** | Registry chỉ load một schema/entity; `schema_version.const` khiến minor bump chưa backward compatible runtime | Thiết kế registry đa phiên bản/upcaster + compatibility test trước ĐA-05/06 migration. |

## ✅ ĐÃ CHECK XONG

| Mã | Ngày | Verdict |
|---|---|---|
| **Q-10** | 2026-07-28 | ✅ **CHỐT (c) rồi (b)** — Cường: *"(c) rồi (b)"*. Nghĩa là: **①** đánh gốc REST của `shift_plan` trước (thủ phạm −33k ở B0), đo lại b4; **②** SAU ĐÓ đổi veto 8 thành *"km rỗng chỉ được tăng nếu tổng payout đội tăng SIG cùng lúc"*. `positioning_overrides` giữ **off** cho tới khi đo lại xong theo veto mới. |
| **V-17 (uỷ quyền)** | 2026-07-28 | ✅ Cường uỷ quyền agent quyết ("2,3 bạn quyết"). **Quyết định của agent**: V-17 ở lại hàng đợi **non-blocking** như V-01..V-16 (nhất quán tiền lệ "hoãn ≠ waive" — không tự waive gate mình vừa tạo); bằng chứng số (HHI giảm SIG, per-seed JSON `21-*`) đủ cho code tiến tiếp, mắt người vẫn cần xem trước bản chốt. |
| **T-045b + ①effective** | 2026-07-28 | ✅ Cường uỷ quyền agent quyết. **Quyết định**: LÀM CẢ HAI trong cycle kế — ① `PolicyBundle` đọc `effective_from/to` + solver nhận `as_of` (rẻ, schema đã bắt buộc trường này, mở đường A1 router-theo-policy); T-045b thêm trường chi phí **mặc định 0** (`swap_fee_vnd`, `battery_rent_vnd_per_month`, `free_swap_until`, `cash_cost_vnd_per_km`) — hành vi mặc định KHÔNG đổi, chỉ mở đường quét độ nhạy. Tách `cost_per_km_vnd` → `pickup_disutility_vnd_per_km` (đổi tên, giữ 3000). |
| **ARCH-B** | 2026-07-27 | ✅ Chọn một canonical run/snapshot, hai projection: simulation demo = dispatcher/researcher; driver app demo + Advisor = một tài xế. |
| **ĐA-01** | 2026-07-27 | ✅ APPROVED-DESIGN shrinkage estimator thống nhất; pooled prior, no 1.0 fallback, 30-seed recalibration. Chưa implement. |
| **ĐA-02** | 2026-07-27 | ✅ APPROVED-DESIGN B1 time-window; B2 vẫn chờ D-POL-05. Chưa implement. |
| **Q-06** | 2026-07-28 | ✅ **CHỐT phương án (b)** — giữ chỉ báo pin nhưng **gắn nhãn MOCK rõ trên UI**. Đã làm (UPDATE-080): nhãn đi **cùng dữ liệu** (`soc_source`/`vehicle_range_km_source` vào contract `driver_state`, required) để Flutter của Khánh không thể quên; badge `MÔ PHỎNG` cạnh `⚡%` + 2 card giải thích *"đây không phải mức pin thật"*; 2 test chặn hồi quy. **Kèm chỉ thị**: pin phải có tác động lên sim — **đã kiểm: CÓ** (41 lần bỏ đơn vì thiếu pin = 3,5%; 15/133 lượt đổi pin THẤT BẠI; chờ tủ tối đa 50′; 0 lần hết pin giữa đường). SOC **đã là** chiều state của DP; 4 sai lệch mô hình + thứ tự nâng cấp ghi ở hồ sơ `13-*` §3.2b. |
| **Q-08** | 2026-07-28 | ✅ **CƯỜNG UỶ QUYỀN AGENT CHỐT** (*"Q-08 cho bạn chốt"*). **Quyết định**: quyết định thiết kế 2026-07-21 (*"baseline B chưa tối ưu là feature"*) **VẪN CÒN HIỆU LỰC**, nhưng bổ sung **ranh giới = TÍNH KHẢ TÍN VẬT LÝ**. Cụ thể: **(i)** unserved trung bình, utilization trung bình, dồn trạm pin → **GIỮ NGUYÊN**, là dư địa hợp lệ cho advisor (advisor sửa được bằng lời khuyên vị trí/thời điểm); **cấm** vặn `actors.n`/`orders_per_day` để cải thiện chúng. **(ii)** Nhưng **đuôi phi thực tế thì phải cắt**: không tài xế thật nào ngồi im **5,6 giờ** trong khi 20% đơn chết. Ngưỡng chốt: **không khoảng chờ nào > 90 phút**, p99 < 60 phút. **(iii)** Lý do phân định: dư địa hợp lệ là thứ advisor **có công cụ để sửa**; đuôi 5,6h là **lỗi thế giới**, advisor không có lệnh nào tạo ra đơn hàng. Đo advisor trên một thế giới sai không cho kết luận dùng được. **(iv)** Hệ quả: T-045d được phép, nhưng **chỉ để cắt đuôi**, không được đụng tới trung bình. Mọi thay đổi phải in ra CẢ served/util/trips để không lặp mẫu lỗi #5. |
| **Q-05** | 2026-07-28 | ✅ **CHỐT** thứ tự mới (Cường: *"chốt q-05"*): ①`candidate_ring_k_max` 6→12 **DONE (UPDATE-079)** → ②khôi phục tỷ lệ đơn/actor + ghi biên giới vào config → ③**T-045a `MarketStateView` + hồi sinh S4** (đòn bẩy duy nhất đẩy được cả hai metric) → ④mở rộng zone liên quận (realism, làm sau cùng). Kèm 2 đính chính đã ghi nhận: "tăng orders_per_day" **không phải** bịa cầu; mở rộng zone **giữ nguyên mật độ** nên **không** giải được thế lưỡng nan served/trips. |
| **ĐA-03** | 2026-07-27 | ✅ APPROVED-DESIGN p_accept + avg_dist personalization; không tự gồm S2-1. Chưa implement. |
| **SPEC-OBJ-V2** | 2026-07-27 | ✅ **DUYỆT** `specs/advisor-objective-model-v2.md` (Cường: *"oke duyệt hết"*) — 5 số hạng chi phí + multi-agent equilibrium + `MarketStateView` + chỉ tiêu kép + thứ tự thực hiện §6. **Bước 1 đã làm** (UPDATE-075). |
| **ĐA-04** | 2026-07-27 | ✅ **DUYỆT** AdviceCadencePolicy dùng chung (anchor theo pha ca; ≥20'/topic; ≤6 proactive/ca; safety queue; một adherence draw cho `(decision_id, material_revision)`; typed suppression). Cường yêu cầu thêm: **làm ở mức khó nhất, khác biệt rõ sim vs UI, phải visualize**. Chưa implement (cycle C3). |
| **ĐA-05** | 2026-07-27 | ✅ **DUYỆT** append-only lifecycle store + rebuildable projections. Cường chốt: **sim để RAM, KHÔNG ghi đè bảng** — chỉ thêm `run_id`, vũ trụ song song không nhân bản bảng. Yêu cầu nâng cấp: UI và sim **cùng đọc một projection** (một luật, một database). Chưa implement (cycle C2). |
| **ĐA-06** | 2026-07-27 | ✅ **CHỐT** (Cường: *"chốt ĐA-06, tuy nhiên có thể sửa nhiều trong tương lai vì còn đang phân vân"*) — AdviceEnvelopeV2 = `list[card]` + per-number provenance + verify per-card fail-closed + driver mini-trace/reviewer full trace + adapter v1. **Nghĩa vụ nhắc-duyệt đã xong, agent không cần nhắc nữa.** Vẫn giữ thứ tự **POLISH → làm sau** T-041 bước 2 và T-042. ⚠ **Đánh dấu DỄ THAY ĐỔI** ⇒ ràng buộc thi công ở T-044 (schema versioned + một adapter duy nhất + không rải hình dạng card ra nhiều consumer). |
| **ĐA-07** | 2026-07-27 | ✅ **DUYỆT** chi phí cơ hội / ràng buộc SOC-vị thế cho `accept_lift` (hồ sơ `06`). Cường chốt kèm: `accept_lift` **giữ TẮT**; `shift_plan` **giữ BẬT + cảnh báo đỏ**, tương lai thử nghiệm lại; **bản cuối trước khi chốt: TẮT để advisor IM LẶNG nếu không hiệu quả**. |
| **ĐA-08** | 2026-07-27 | ✅ **DUYỆT** chỉ tiêu chấp nhận KÉP (cá nhân + hệ thống + khách + công bằng + tập trung), `coverage: all`, ≥30 seed. **Bước đo đã xong** (UPDATE-075); ràng buộc CI còn lại. |
| **ĐA-09** | 2026-07-27 | ✅ **DUYỆT** multi-agent equilibrium (best-response/fictitious play, price of anarchy, quét ngưỡng phủ) + `MarketStateView` mở kênh thông tin CUNG cho solver, robust ở 3 mức `available/degraded/absent`. Spec §2–§3. Chưa implement (cycle C4 bước 4-5). **Diễn giải của agent** — nếu Cường đánh số ĐA-09 cho việc khác, sửa lại dòng này. |
