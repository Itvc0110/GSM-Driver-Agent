# DEFERRED — Các mục đã hoãn

Cập nhật: 2026-07-27. Mọi ý tưởng/việc ngoài minimum scope phải ghi vào đây (kèm lý do + điều kiện mở lại) thay vì tự triển khai. Không xóa mục — chuyển trạng thái khi mở lại.

| ID | Mục | Lý do defer | Điều kiện mở lại | Trạng thái |
| --- | --- | --- | --- | --- |
| D-001 | Toàn bộ pack cũ theo hướng full multi-variable constrained optimization: `docs/00–09`, `contracts/*.schema.json`, `templates/`, `MASTER_PROMPT.md`, `AGENTS.md` (bản cũ) | Stakeholder muốn tiếp cận top-down, chưa mô hình hóa từ atomic features; scope thu hẹp về 4 khối F0–F3 | Khi cần formal hóa optimizer/contract đầy đủ; nhiều nguyên tắc (ranh giới dispatch, mock labeling, LLM boundary) vẫn được kế thừa trong `CLAUDE.md` | DEFERRED |
| D-002 | State pin xe trong F2 (trong ca) | Chưa cần cho khung tối thiểu; mock đơn giản trước | Khi F2 chạy ổn với state đơn khu vực | DEFERRED |
| D-003 | Chi tiết UI/độ hoàn thiện giao diện F1 | Ưu tiên khung tính năng trước; UI tạm dùng bản clone | Sau khi có bản clone (T-009) và user story được xác nhận | DEFERRED |
| D-004 | Phase-2 kiểu cũ: gợi ý STAY_IN_ZONE / REPOSITION_TO_ZONE, fleet capacity, fairness allocator **trong SẢN PHẨM** | Thuộc hướng cũ, vượt xa minimum scope; rủi ro can thiệp cung–cầu. **Nới một phần 2026-07-21 (Cường, yêu cầu #5):** fleet-awareness (supply field, capacity ledger trạm pin, anti-herding, staggering) được dùng **trong phạm vi simulator/advisor-sim** để nghiên cứu rủi ro hệ thống — `specs/simulation-twin-world.md` §6; advice sản phẩm tới tài xế vẫn không reposition | Product: khi có yêu cầu stakeholder + dữ liệu fleet thật | DEFERRED (product) / OPEN (sim) |
| D-005 | Đánh số phase & mâu thuẫn MASTER_PROMPT §13 vs docs/07 (audit 2026-07-20 xác nhận) | Cả hai file đã defer nên không cần sửa ngay | Nếu mở lại pack cũ thì phải chốt docs/07 làm source of truth và đánh số lại | DEFERRED |
| D-006 | Luồng giải trình vi phạm (drawio `GSM_Income_AI_agent.drawio`) | Quyết định của Cường 2026-07-20: là một **dự án khác**, không thuộc repo này; file drawio đã **xóa khỏi repo** theo yêu cầu (chưa từng commit git — khi cần lấy lại từ nguồn drawio gốc của team) | Khi dự án giải trình vi phạm khởi động ở repo riêng | OUT-OF-SCOPE |
| D-007 | Quy trình khiếu nại/giải trình khi tài xế bị phạt/trừ tiền (biểu mẫu, SLA, kháng nghị) | Cường 2026-07-20: bài toán khác (liên quan D-006); không research/build tiếp trong repo này | Cùng điều kiện D-006 | DEFERRED |
| D-008 | Tích hợp **nguồn cộng đồng** (group tài xế, websearch kinh nghiệm) vào sản phẩm | Tính năng **tương lai/roadmap** sau khi hoàn thiện F0–F3; rủi ro tin sai/lỗi thời/PII → phải qua khối kiểm chứng & lọc rủi ro trước | Sau khi F0–F3 chạy ổn; triển khai theo `specs/community-source-risk-control.md` | ROADMAP |
| D-009 | Đa loại đơn trong sim generator (chở khách + Xanh Express giao hàng + Xanh SM Ngon đồ ăn, policy khác nhau theo cohort) | Pilot v0 chỉ sinh 1 loại đơn để đơn giản; action-space đã ghi nhận 3 loại có thật | Sau khi pilot pass DoD-core; là hướng làm giàu tự nhiên (audit insight 6) | DEFERRED |
| D-010 | Trong sim, các hạng mục sau pilot: kịch bản tuần + trust dynamics, counterfactual branch re-run, regime sweep orders∈{900,1200,1800}, mất điện 1 trạm, adoption 70%, mở rộng res 8/N=500, ĐBTN cohort trong policy bundle | Chương trình reliability-first 2026-07-22 mở có chọn lọc, không mở toàn bộ scope cùng lúc | **MỞ qua T-034/T-027:** demand regime + labeled distribution/environment shifts; station outage/adoption khi evaluator sẵn sàng. **VẪN DEFER:** tuần+trust, full counterfactual branch, res8/N=500, ĐBTN cohort — mở sau M4 eval gate hoặc khi có bằng chứng riêng | PARTIAL REOPEN |
| D-GCP-01 | **BigQuery/GCP + mọi hạ tầng cloud** (DataSource live, service-account, cost guard) | **Quyết định Cường 2026-07-24: sản phẩm publish CHẠY TRÊN MOCK DATA.** Schema/tên bảng/tên cột là THẬT (GSM cấp), chỉ NỘI DUNG do ta gen. Hạ tầng local hết (DB/cache/vectordb) | Khi ghép vào data thật của GSM — plan PI-3 (`specs/real-data/04-datapull-tool-plan.md`) đã viết sẵn, chỉ cần bật | DEFERRED (chủ ý) |
| D-LOCAL-01 | Hạ tầng chạy LOCAL: parquet/DuckDB thay warehouse, cache file/sqlite, vectordb local (nếu cần RAG) | Cường 2026-07-24: "tạm thời dùng local hết" | Khi lên production/ghép data thật | ACTIVE (đang áp dụng) |
| D-EXT-02 | **Google Maps không cần cho stack hiện hành** (key đang có cũng KHÔNG hợp lệ: `REQUEST_DENIED`, 64 hex ≠ định dạng Google ~39 ký tự `AIza…`) | Cường đã chốt 2026-07-26: OSRM + Stadia + OSM đủ cho routing/map/geocode mục tiêu hiện tại | Chỉ mở lại nếu một use case đã duyệt không thể đáp ứng bằng stack hiện hành **và** key/chi phí được duyệt | **CLOSED 2026-07-26** / SUPERSEDED (OSRM + Stadia) |
| D-C6-01 | F0 KB retrieval relevance/ranking — query "thưởng" hiện cite "Bộ quy tắc ứng xử" thay vì chính sách thu nhập (keyword count trên 7 record, chưa vector) | C6 minimum: số F0 vẫn từ solver (truy vết được), citation là policy thật; relevance là tinh chỉnh | Khi F0 lên UI thật hoặc có eval set citation-relevance (C7) | DEFERRED (sev thấp) |
| D-C6-02 | F0 template luôn nêu bonus-gap dù câu hỏi thuần policy → tách intent "policy-lookup" (chỉ trích dẫn) vs "bonus-gap" (có số) | Template fallback tối thiểu 1 khuôn/feature; LLM composer xử lý sắc thái tốt hơn | Khi F0 free-text đa dạng / có eval set intent | DEFERRED (sev TB) |
| D-C6-03 | Chạy `scripts/smoke_advisor_live.py` với LLM THẬT (composer/verifier/repair live) | Chưa cài `openai`; endpoint gpt-4o-mini từng 403 (T-025); test-suite deterministic không gọi mạng | `uv sync --extra llm` + `.env` + endpoint sống | TODO (sev TB) |
| D-C6-04 | Solver đăng ký MỌI số surfaced (kể cả khung giờ trong `problem_digest`) vào `numbers[]` để bỏ phụ thuộc `_trusted_spans` khi verify | Hiện digest là text solver deterministic, tin được; nhưng số trong digest chưa ở `numbers[]` | Khi mở rộng solver / siết faithfulness structural tuyệt đối | DEFERRED (sev thấp) |
| D-POL-01 | **MODEL GAP: khoán tuần + clawback 20-40%.** S5 WeeklyKhoanFeasibility và pipeline mapping đã có ở UPDATE-032/038/040, nhưng active GSM quota/clawback numbers chưa có | Code path/model shape đã làm; policy values vẫn image-locked/data-blocked | D-POL-05 khi GSM cung cấp active numbers | DONE-CODE / BLOCKED D-POL-05 |
| D-POL-02 | **SCHEMA GAP:** service_type/weekly_quota/clawback fields | Additive schema fields and solver/report wiring đã có ở UPDATE-032/038/039 | D-POL-05 để xác nhận semantics/values thật | DONE-CODE / BLOCKED D-POL-05 |
| D-POL-03 | **MOCK regen:** policy bundle/ledger phản ánh model weekly/service-point | Mock/schema regen đã được re-ground trong UPDATE-034..040; exact GSM policy figures còn thiếu | D-POL-05 và policy owner confirmation | PARTIAL / BLOCKED D-POL-05 |
| D-POL-04 | **CORPUS GAP (owner Khánh, không tự sửa):** T-004 corpus thiếu record **Vận Doanh 23/02/2026** (bỏ phạt ≤70% + khoán tuần) và chưa gắn cờ **mâu thuẫn** với Bộ QTƯX 05/06/2026. F0 hiện có thể cite policy cũ | Corpus là file của Khánh (T-004); tôn trọng ownership | Khánh thêm record + effective_date + conflict flag; hoặc Cường ủy quyền | TODO (sev cao — F0 correctness) |
| D-POL-05 | **Image-locked numbers → pull data thật GSM:** khoán tuần tối thiểu (VND/tuần), clawback_rate active theo market, bảng mốc điểm mới nhất, điểm/cuốc theo service_type, thâm niên/Loyalty Bike, cohort ĐBTN sau 07/2026 | Số chỉ có trong ảnh official; partnership cho quyền data thật | Khi wire được data thật GSM (thay vì web/OCR). **2026-07-24: schema thật đã có (UPDATE-033); số target vẫn cần GSM chỉ chỗ (P1§4). Hợp nhất vào roadmap PI-1..6.** | DEFERRED (sev TB — resolvable) |
| D-POL-06 | **Active GSM fare/share chưa xác định theo track, market, service và effective date**; `sim-policy-v0` chỉ là bundle MOCK cho Simulator/Web demo | Active value source/effective date chưa được cung cấp; không suy diễn từ research hoặc giá hard-code cũ | GSM first-party source + policy owner approval + versioned migration/compatibility test | DEFERRED (sev cao — không dùng làm production policy) |
| D-004b | **Reposition/idle-reduction MỞ LẠI CÓ ĐIỀU KIỆN** (UC5): data thật `public_driver_hex_tracking` có `target_hex`/reposition mission ⇒ GSM tự có cơ chế reposition → tư vấn theo mission GSM (không tự bịa heatmap), capacity-aware chống dồn cung, nhãn PROXY demand | D-004 (product) cấm reposition; nay GSM có cơ chế chính thức | Impl ở PI-5 (P6 idle-reduction); giữ 5 điều kiện an toàn action-space | REOPEN có điều kiện |
| D-EXT-01 | **ExternalContext runtime** — WeatherAPI + OSRM + Stadia + Jina; holiday/event/traffic chỉ thêm khi có use case | Config/key đã có; ngoài OSRM route/cache, Advisor chưa đọc external context và chưa có scheduler/TTL/freshness policy | Mở cycle provider/cache/freshness riêng sau khi canonical projection B ổn; fixture deterministic, nhãn PROXY/EXTERNAL, không gọi live trong sim loop | DEFERRED (không còn blocked bởi key/Google) |
| D-SIM-01 | **F-SIM1-A: `trips/driver/day` ≈ 12.3 thấp hơn research 18-22.** Nguyên nhân CƠ CẤU, không phải bug: cầu 1 quận Đống Đa = 1200 đơn/ngày ⇒ trần tuyệt đối 1200/74 ≈ 16 cuốc/tài xế kể cả phục vụ 100%. Thực tế tài xế chạy LIÊN QUẬN | `served` (realism HỆ THỐNG — khuyết tật Cường chỉ ra) và `trips/driver` (realism CÁ NHÂN) **không thể cùng đạt** với cầu 1 quận; SIM-1 ưu tiên `served`. Mở rộng zone đã được Cường DEFER trước đó | Khi enlarge zone (nhiều quận) → đo lại cả 2; hoặc khi có data thật GSM về số cuốc/tài xế/ngày ở Đống Đa | DEFERRED (sev TB — giới hạn phạm vi, đã ghi rõ trong config) |
| D-SIM-02 | **F-SIM2-A: sim cho tài xế MỚI thưởng 0đ.** Newbie/rating/mission mechanism đã được thêm trong SIM-XANH P2; proxy-value limitation và active policy data vẫn còn | Code baseline đã được vá, nhưng không claim policy accuracy khi D-POL-05 chưa có số GSM thật | D-POL-05 + V-03/V-09 verdicts | DONE-CODE / BLOCKED D-POL-05 |
| D-SIM-03 | **F-SIM3-A: cầu nối advice→action mới phủ 1/9 solver (⚠ 2026-07-29: nay 4 kênh — S2 shift_plan · S1-gate accept_lift · S7 rest_window · S4 positioning/standby, `advice_bridge.py`; PARTIAL-RESOLVED, còn S3/S5/S6/S8/S9 chưa có kênh).** Chỉ S2 `shift_dp` sinh ra hành động nằm trong action-space của actor; 8 solver còn lại (S1 bonus, S6 mission, S7 idle…) ra lời khuyên THÔNG TIN hoặc về hành vi NHẬN ĐƠN mà actor chưa có kênh thực thi | Đo được: Δ(B−A) ≈ −3.173đ (nhiễu) vì solver nói ONLINE 61/64 lần. **Rủi ro diễn giải**: SIM-4 chỉ đo với S2 sẽ đánh giá THẤP advisor một cách hệ thống | Cycle riêng mở rộng action-space (vd advice điều biến `accept_base` tạm thời, chọn mission). Làm SAU SIM-4 để có baseline so sánh | **PARTIAL-RESOLVED (sev TB)** — 4 kênh đã có, còn S3/S5/S6/S8/S9 |
| D-SIM-04 | **F-SIM3-B: `adherence_by_archetype` là ASSUMPTION thuần lập luận** (tân binh 0.75 / lão làng 0.30), chưa có dữ liệu | Mọi kết luận SIM-4 kiểu *'advisor giúp tân binh nhiều nhất'* sẽ THỪA HƯỞNG giả định này | Khảo sát tài xế thật hoặc A/B thật; hoặc phân tích độ nhạy | ✅ ĐỊNH LƯỢNG XONG 2026-07-26 (UPDATE-056): adherence 0.3 giết significance ở MỌI lift — dưới 0.75 đừng hứa hẹn; số thật vẫn cần để thay giả định |
| D-SIM-05 | **F-SIM4-B: chưa chặn lời khuyên CÓ HẠI.** Đã chứng minh tuân thủ nửa vời gây lỗ 34k (nâng tỷ lệ nhưng không chạm ngưỡng ⇒ nhận cuốc rẻ, không có thưởng bù), nhưng bridge vẫn khuyên bất cứ khi nào acceptance < ngưỡng | Thưởng theo NGƯỠNG là được-ăn-cả-ngã-về-không; khuyên nửa vời tệ hơn không khuyên | Thêm điều kiện KHẢ THI (kiểu S1 bonus_feasibility) trước khi đưa kênh này ra thật. **Nên làm trước SIM-5** | ✅ **DONE 2026-07-26 (UPDATE-048)** — đã cài, nhưng đo lại thấy INERT ở config hiện tại; tiền đề 'vách đá' cũng đã bị BÁC BỎ bằng 30 seed |
| D-SIM-06 | **F-SIM4-A/C: kết luận SIM-4 mới trên 1 tài xế P4, 1 config; `accept_lift_step/max` là ASSUMPTION** | UPDATE-056 completed sensitivity across archetypes/adherence/lifts; result remains proxy, not universal uplift | Replace assumptions with real data when available | DONE-CODE / PROXY-LIMITED |
| D-SIM-07 | **F-DSIM05-A/B: `max_realized_accept` (0.93) và `accept_lift_max` (0.15) là hai tham số ĐỘC LẬP nhưng phải nhất quán**; và điều kiện khả thi chưa được kiểm ở vùng nó THỰC SỰ bind (vào ca muộn, ngày đã hỏng) | Lệch nhau thì điều kiện khả thi đánh giá sai năng lực thật của tài xế ⇒ hoặc chặn nhầm, hoặc cho qua nhầm | Suy `max_realized_accept` từ đo theo `accept_lift_max`, hoặc thêm kiểm tra nhất quán; thêm kịch bản end-to-end cho vùng bind | DEFERRED (sev TB) |
| D-SIM-08 | **F-SIM5-A: `verify_realdata_stats.py` sinh data RIÊNG (30 seed × 1 ngày), không đọc bộ 90 ngày đang nằm trên đĩa** | Vòng 2 đang kiểm GENERATOR chứ không kiểm ARTEFACT. Hiện trùng nhau vì cùng engine, nhưng sẽ lệch nếu regen bằng config khác | Thêm chế độ `--from-dir` đọc thẳng thư mục data | DEFERRED (sev TB) |
| D-SIM-09 | **F-DSIM03-A: `check_bonus_gate` TỰ CÀI LẠI lý lẽ advisor** (so ngưỡng, tính khả thi) thay vì gọi solver **S1 `bonus_feasibility`** | Đúng lỗi 'hai nguồn sự thật' mà SIM-1 phải đi sửa: sim và advisor thật có thể lệch lý lẽ mà không ai biết. Kênh S7 (UPDATE-050) đã gọi solver thật để không lặp lỗi | Dựng `bonus_gap_input` từ trạng thái sim → gọi `bonus_feasibility.solve()` → dùng `solution.feasible` + `infeasible_reason` | ✅ **DONE 2026-07-26 (UPDATE-051)** — Δ mean giảm +32.276→+20.473 NHƯNG lỗ chỉ còn 4/30 seed (advisor im lặng ở 16/30 ca không giúp được), tỷ lệ lợi/hại 5,7:1 |
| D-SIM-10 | **F-DSIM03-B + PHÁT HIỆN D-SIM03-A: sim mới MỘT NGÀY nên phần lớn solver không có kênh tác động.** S2 `shift_dp` là solver DUY NHẤT nhìn về phía trước; S3/S7/S8/S9 hồi cứu, S5 khoán TUẦN, S6 cần mô hình mission | Đo được: kênh `rest_window` (S7) INERT vì khung nó chỉ ra LUÔN nằm phía sau — lời khuyên hồi cứu về bản chất là cho NGÀY MAI | Sim **nhiều ngày**: advice ngày N → hành vi ngày N+1. Mở khoá S3/S5/S7/S8/S9 cùng lúc + là điều kiện cần cho khoán tuần. **Đòn bẩy LỚN NHẤT cho action-space** | ✅ **DONE 2026-07-26 (UPDATE-052)** — `run_multiday` + `DriverMemory`; S7 HẾT INERT (0 event/ngày → có event từ ngày 2). Còn F-DSIM10-A..D |
| D-SIM-11 | **F-DSIM09-A: sim nhận diện nguyên nhân nghẽn bằng CHUỖI TIẾNG VIỆT** (`"quỹ" in infeasible_reason`, `"tỷ lệ nhận" in ...`) | Đổi câu chữ trong solver S1 sẽ **âm thầm** làm hỏng gate — kênh advice sẽ khuyên sai ca mà không test nào đỏ | S1 trả mã lý do có CẤU TRÚC (vd `blockers: ["hours","acceptance","completion"]`) thay vì để tầng khác parse text | TODO (sev TB) |
| D-SIM-12 | **F-DSIM09-B: còn 4/30 seed advice vẫn gây LỖ** (−131.210đ tổng) | Điều kiện khả thi đã lọc phần lớn ca vô ích nhưng chưa hết | Không mở riêng; dùng sweep/calibration D-SIM-06 và ĐA-01 để đánh giá lại | ✅ THAY THẾ bởi sweep D-SIM-06 (UPDATE-056): bức tranh mới n_pos 14-16/30, đuôi thua nhỏ, thắng khi flip bậc thưởng. Giữ finding gốc cho audit |
| D-SIM-13 | **F-DSIM10-B/C/D (sau khi có sim nhiều ngày)**: (B) `DriverMemory` chưa nối vào `build_bonus_gap_input` nên S1 vẫn nhận ước lượng TRONG-NGÀY thay vì lịch sử thật; (C) tích luỹ TUẦN chưa reset theo chu kỳ ⇒ chỉ đúng khi `days ≤ 7`; (D) `mockgen` vẫn sinh 90 ngày ĐỘC LẬP, chưa dùng `run_multiday` | (B) mất giá trị chính của nhiều ngày; (C) sai số khi chạy chuỗi dài, **chặn** S5 khoán tuần; (D) data 90 ngày vẫn không có tính liên tục ngày-qua-ngày | B trước (việc nhỏ, giá trị rõ) → C (trước khi nối S5) → D | ✅ **DONE 2026-07-26 (UPDATE-053)** — cả B/C/D + 11 fix từ review đối kháng 28-agent |
| D-SIM-14 | **REVIEW-C2: memory đổi việc CÓ/KHÔNG rút coin adherence ⇒ reorder dòng `bridge.rng` tuần tự dùng chung** — so sánh memory-on/off lẫn hiệu ứng thông tin với việc xáo coin | Mọi phép đo 'giá trị của memory' bị nhiễu bởi decorrelation không liên quan | Rút coin adherence theo KHOÁ `(seed, actor_id, day, consult_index)` thay vì stream tuần tự — mỗi quyết định một coin riêng, bật/tắt tính năng không xáo coin của quyết định khác (⚠ 2026-07-29: vật mang khoá ĐÃ CÓ — `decision_id` 7 điểm emit, Cycle W; kênh standby đã rút MỘT lần lúc gán từ UPDATE-083; phạm vi còn lại = các kênh tick khác) | ✅ **DONE-CODE 2026-07-29 (ĐA-04, UPDATE-099)** — `adherence_coin(seed, decision_id, material_revision)` sha256, MỘT coin/quyết định ở cả 4 kênh; test `test_recheck_does_not_reroll` canh. Bật/tắt tính năng không còn xáo coin của quyết định khác. Chờ Cường check (V-18) |
| D-SIM-15 | **REVIEW-C15 (pre-existing): ledger `day_bonus` của actor còn online cuối ngày bị đóng dấu 00:00 NGÀY HÔM SAU** ⇒ trong dataset liên tục, thưởng ngày N rơi vào bucket ngày N+1 với consumer group-by-date | Sai lệch nhỏ nhưng có hệ thống ở phân tích theo ngày | Đóng dấu 23:59:59 của đúng ngày settle | DEFERRED (sev THẤP) |
| D-SIM-16 | **Autocorr cuốc/ngày ≈ −0.04 dù đã có chuỗi liên tục** — vì không có trạng thái HÀNH VI nào sống qua đêm (SOC hồi, mệt reset; giữa các tài xế thì between-sd 3.88 > within 3.9... đã tách). Tính liên tục hiện = danh tính + memory + kế hoạch, CHƯA = persistence hành vi | Data thật có ngày nghỉ, thói quen, ốm, chu kỳ tuần cá nhân — mock thiếu chiều này | Mô hình persistence: xác suất nghỉ cả ngày theo cá nhân, mệt tích luỹ đa ngày, thói quen khung giờ trôi chậm | DEFERRED (sev TB — cân nhắc trước bản publish nếu phân tích chuỗi thời gian là use-case) |
| D-SIM-18 | **F-P3-B: ước lượng tỷ lệ nhận cho gate advice nhảy giữa 2 chế độ** (base/memory khi <5 offer; realized THÔ khi ≥5 — mẫu 4-5 offer quantize 0.75/0.80 ⇒ advice theo NHIỄU) | Đo được: P2 (base 0.95) nhận advice khi chuỗi xui — mà điều này ĐÚNG sản phẩm (thưởng chấm realized) nên câu hỏi là ƯỚC LƯỢNG chuẩn, không phải tắt advice | **Chuyển cho MATH AUDIT** (đúng loại câu hỏi DIRECTIVES §4): cân nhắc shrinkage estimator (prior = memory, trọng số cỡ mẫu) | TODO (audit) |
| D-UI-01 | **Nghỉ hưu dashboard Streamlit + `ui/simulator_ui/`** sau khi khu "Mô phỏng" trong web UI đạt parity — hiện dashboard vẫn là công cụ visual gate V-09 và là bản tham chiếu port U3 | Gỡ sớm thì mất công cụ đối chiếu số web-vs-engine và mất chỗ Cường check V-09 | Chờ Cường duyệt **V-10** (khu Mô phỏng web) → cycle dọn riêng: gỡ dashboard.py + dashboard_theme.py hoặc chuyển vào archive | DEFERRED (sev THẤP — quyết sau V-10) |
| D-UI-02 | **Web UI phụ thuộc CDN** (Tailwind/Leaflet/Plotly.js/FontAwesome/Google Fonts) — cần internet lúc mở trang; theo stack sẵn của Khánh | Demo offline hoặc mạng chặn CDN sẽ vỡ style/chart | Vendor hoá assets về `ui/web/vendor/` (tải 1 lần, commit) khi UI ổn định — làm trong U4 hoặc cycle riêng | DEFERRED (sev THẤP) |
| D-SIM-19 | **AUDIT A1 DEMAND-1: cờ `demand.hour_interp` là cờ chết đã gỡ** — sim KHÔNG nội suy cường độ giữa mốc giờ (răng cưa tại biên giờ tồn tại thật) | Phân phối thời gian đặt đơn có bậc thang theo giờ; ảnh hưởng phân tích theo phút quanh biên giờ | Cài nội suy thật (lam(t) tuyến tính giữa mốc) + regate realism 30 seed vì đổi phân phối cầu | DEFERRED (sev THẤP) |
| D-A3-01 | **CADENCE-2 adherence washout (CAO)**: coin tuân thủ được rút LẠI mỗi idle-tick (2') cho tới khi "thành công" ⇒ adherence danh nghĩa 0.3 có hiệu dụng ≈1.0; cộng với BRIDGE-3 (41/70 "followed" là advice NO-OP) ⇒ **mọi con số adherence trong kết luận A/B đang bị thổi** | Δ A/B và sweep D-SIM-06 đo trên adherence không đúng như khai báo — kết luận "advisor giúp X" thừa hưởng sai số này | Gộp với D-SIM-14 (coin theo khoá) + ĐA-04 cadence: một coin/quyết định/khe thời gian, không re-roll; loại advice NO-OP khỏi đếm followed (⚠ 2026-07-29: phần THƯỚC đã đóng — Cycle W pin adherence theo ground truth, hai tên decision/event; phần còn hở đúng là re-roll idle-tick + NO-OP) | ✅ **DONE-CODE 2026-07-29 (ĐA-04, UPDATE-099)** — re-roll idle-tick CHẾT: hai đơn vị đo hội tụ (decision 68,1% vs event 67,6%; trước 76,9 vs 53,6). ⚠ **Phần NO-OP (BRIDGE-3: advice không đổi hành vi vẫn đếm `followed`) VẪN HỞ** — không thuộc cycle này, giữ mở dưới D-A3-01b |
| D-A3-02 | **MEMSTATE-1**: `completion_hist` ghi nhưng CHẾT — S1 vẫn nhận completion_rate=1.0 thoái hoá đầu ca (họ hàng EST-1) | Advisor khuyên sai địa chỉ (nâng tỷ lệ NHẬN trong khi nghẽn là HOÀN THÀNH) | Nối completion_hist vào bridge; gộp vào ĐA-01 (estimator thống nhất) (⚠ 2026-07-29: MEMSTATE-2/3/4/6 đã đóng ở Cycle W — MEMSTATE-1 là mục cuối của nhóm) | TODO (sev TB) |
| D-A3-03 | **TIMEENG-1/2/3**: hoạt động censor cuối ngày bị dán nhãn "idle"; `time.warmup_min` knob chết; idle có HAI nguồn sự thật (counter pre-credit vs segment) | Metric idle/utilization mà dashboard + kênh rest_window/S7 đang đọc không tin được | Thứ tự đề xuất của auditor: kế toán thời gian & nhãn censor TRƯỚC (rẻ, narrow), rồi quyết warmup implement-hay-gỡ | TODO (sev TB) |
| D-A3-04 | **MEMSTATE-2/5 + ĐA-05**: EpisodeStore write-only; 3 store trạng thái advice không join được (episode uuid · UI jsonl · sim events) ⇒ vòng adherence §12 HỞ (nút Bỏ qua không đổi hành vi advisor) | Không đo được vòng kín hiểu-và-đo; advice bị bỏ qua vẫn lặp lại | Chuẩn hoá advice_id content-addressable + chọn store canonical (đề án ĐA-05) | ✅ **DONE-CODE 2026-07-29 (Cycle W, UPDATE-091)** — event log canonical + projections một-luật + 3 namespace ID join được qua `decision_id`. Vòng adherence **đã kín về hạ tầng**; biến nó thành thay-đổi-hành-vi (advice bị bỏ qua thì im) là việc của **ĐA-04 cadence** |
| D-ARCH-A2 | **Cache kết quả solver theo `problem_digest`** (ý tưởng Cường 2026-07-28, `OPEN-THREADS` §A2): khoá cache = digest bài toán + `policy_version` + bucket `as_of` thay vì khoá theo *câu hỏi* như `advice_cache` hiện nay | 90 tài xế cùng ô/bucket/policy đang giải lặp cùng một bài; policy đổi không tự invalidate | Dùng `SolverReport.problem_digest` (đã có, chưa ai dùng làm khoá) — ⚠ bẫy: quên `as_of` ⇒ lời khuyên buổi sáng dùng lại buổi tối | DEFERRED (sev TB — cycle riêng; Cycle W cố ý KHÔNG nuôi thêm `advice_cache` cũ) |
| D-A3-05 | **LAYEROUT-3/6/7/11/12 (giọng văn)**: digest máy làm câu mở đầu F1; F3 tự mâu thuẫn; hai loại "mốc thưởng" cạnh nhau; caveats solver bị vứt; recap không nói số tài xế quan tâm nhất | Tài xế đọc thấy máy móc/khó hiểu — ảnh hưởng noticing & following (mục tiêu §12) | Gộp vào ĐA-06 (template trả list[card], một giọng nói, xưng hô thống nhất) | TODO (sev TB) |
| D-A3-06 | **ROUTERKB-1/2/5**: router substring không word-boundary (còn cặp đồng âm chưa fix, cổng R5 thủng); KB ranking TF thô; query rỗng → citation theo alphabet | Dưới §12 F0 thành FAQ tối giản nên rủi ro giảm, nhưng vẫn sai khi còn dùng | Làm cùng cycle F0-FAQ (chốt legacy hoá router free-text — ROUTERKB-7) | TODO (sev TB) |
| D-ĐA04-01 | `material_revision` rỗng/None vẫn tính được `adherence_coin` (chuỗi rỗng) — KHÔNG fail-loud | Hai lời khuyên nội dung khác nhau có thể dùng chung coin mà không ai biết; đúng họ "hidden fallback" đã cắn ở B3 | Rủi ro hiện THẤP (4 call site đều truyền hằng số). Mở lại khi thêm call site có revision tính động ⇒ khi đó phải raise thay vì nhận rỗng | DEFERRED (sev THẤP) |
| D-ĐA04-02 | Tuning `min_gap_min_per_topic=20′` và `max_proactive_per_shift=6` bằng telemetry thật | Hai hằng số này là baseline duyệt, chưa có dữ liệu người thật đỡ lưng ⇒ mọi số hiệu năng của UPDATE-099 **có điều kiện theo chúng** | Design ĐA-04 đã nói "tuning sau dựa telemetry". Mở lại khi có ≥1 tuần dữ liệu nút bấm THẬT (hiện chỉ MOCK) | DEFERRED (sev TB) |
| D-A3-01b | **BRIDGE-3 tách ra từ D-A3-01**: advice NO-OP (lời khuyên không đổi hành vi thực tế) vẫn được đếm là `followed` — 41/70 ở lần đo cũ | Adherence vẫn cao hơn thực chất dù washout đã chết; Δ A/B thừa hưởng phần này | Cần định nghĩa "advice có tác dụng" ở tầng bridge (so hành vi có-vs-không lời khuyên trong cùng tick) rồi loại NO-OP khỏi tử số. Mở lại ngay khi cần công bố con số adherence tuyệt đối | TODO (sev TB — kế thừa từ D-A3-01) |
| D-ĐA04-03 | **Ngân sách 6/ca bị chiếm theo FIFO, không theo giá trị** — ⚠ con số nén dưới đây phóng đại ~47% (xem `D-R08`); đo seed 1000 ladder all: 531 lần NÓI vs 2.670 lần NÉN (83%); actor 89 có 6/6 suất ngân sách bị `shift_plan` (kênh ĐA-07 kết luận ÂM) lấy; ~~`rest_window` không nói được lần nào vì chết đói~~ **← SAI, đã bác bỏ**: 231 lần nén đó 100% là ma; tắt hẳn cadence nó vẫn nói 0 lần ⇒ im vì không có khung để hoãn | Kênh giá trị cao bị kênh giá trị thấp bỏ đói; và mọi số Δ A/B của cadence hiện phụ thuộc vào việc kênh nào tình cờ hỏi trước | Cần cơ chế ưu tiên/đấu giá khi tranh ngân sách (design ĐA-04 chỉ định nghĩa priority cho safety bypass). **Phải qua plan mode — là thiết kế mới, không phải fix.** ⚠ **ĐÃ CHỨNG MINH NHÂN QUẢ bằng lưới 2×2 (artifact 33+34, 30 seed CRN): bỏ `shift_plan` khi cadence TẮT đổi −25đ (vô hại), khi cadence BẬT đổi +1.433đ ⇒ **tương tác +1.458đ** nằm trọn ở việc nó CHIẾM SUẤT ngân sách, không phải ở lời khuyên nó đưa. FIFO đang tốn ~1.458đ/tài xế/ngày.** Đây cũng là bằng chứng độc lập #4 cho ĐA-07 (shift_plan không chỉ tự âm, còn bỏ đói kênh dương) | TODO (sev **TB cho production HÔM NAY · CHẶN CHÍNH cho mọi việc bật lại kênh** — cập nhật 2026-07-29 sau artifact 37. Giữ TB vì ở bản ship `channels` 4 kênh đều **false** theo ĐA-07 và positioning nằm NGOÀI hệ thống cadence ⇒ FIFO tốn ≈0đ hôm nay. NHƯNG artifact 37 cho thấy đây là nơi **TOÀN BỘ** chi phí của nhịp tập trung: bỏ `shift_plan` khi cadence ON đáng +1.549đ, khi OFF đáng −1.700đ ⇒ **tương tác +3.249đ**, và không có `shift_plan` thì nhịp gần như miễn phí (+384đ). Tức: **nhịp không đắt — cách chia ngân sách mới đắt.** Bất kỳ quyết định bật lại kênh nào cũng phải giải cái này trước. 4 phương án + bảng chấm độc lập ở `tracking/PLAN-d-da04-03-budget-priority-DRAFT.md`; chờ Q-09 chốt trước) |
| D-ĐA04-04 | Event `advice_suppressed` nặng gấp ~5 lần event lời khuyên thật (2.670 vs 531 ở một run) | Chấp nhận ở sim (RAM, đúng verdict ĐA-05); KHÔNG chấp nhận nếu bê sang sản phẩm ghi SQLite | Nén thành counter theo (driver, topic, reason, pha) thay vì một event/bucket. Mở lại khi UI bắt đầu ghi suppressed vào store thật. ⚠ Đã có tác dụng phụ đo được: full suite chậm từ 17′37″ → 21′19″ | DEFERRED (sev THẤP) |
| D-F098-A | **Artifact 29 (sweep chi phí B2) và 30 (C5 swap fee) đo bằng solver TRƯỚC fix F-098-01** — gate Bellman cũ loại nhánh ONLINE có tổng value dương | Độ lớn của break-even chi phí (≈1.570đ/km) và của Δ `s2_only` có thể đổi. Chiều kết luận KHÔNG đảo (fix chỉ THÊM nhánh khả thi, không bỏ nhánh nào) | ✅ **Đã spot-check DẤU 2026-07-29** (5 seed, cash 0 và 250đ/km, sau TOÀN BỘ bộ fix): Δ net vẫn **DƯƠNG ở cả hai mức** (+2.164đ và +1.629đ) ⇒ kết luận *"positioning dương ở mọi mức chi phí thực"* **KHÔNG đảo**. ⚠ Điểm ước lượng thấp hơn artifact 29 (+4.000 / +3.363) nhưng **KHÔNG so được**: n=5 vs n=30, CI rất rộng, và chênh này phản ánh CẢ bộ fix (DET-01/R-01/R-09) chứ không riêng F-098-01. Chạy lại đầy đủ chỉ cần khi trích ĐỘ LỚN (vd break-even ≈1.570đ/km) để ra quyết định | TODO (sev THẤP — dấu đã xác nhận) |
| D-F098-B | **F-098-03/04/05 của debate review chưa xử lý**: (03) 15 schema có `date-time` nhưng `Draft202012Validator` không có `format_checker` ⇒ `2029-02-31` lọt; (04) B3 policy-costs chưa nối canonical sim bridge; (05) taxonomy `track` trộn employment với energy/fleet + `from_record` bỏ `source/source_url` | (03) timestamp rác vào store append-only; (04) không được claim "policy đang điều khiển advisor runtime"; (05) có thể resolve sai cost hoặc trình bày MOCK như official | (03) cần plan mode — đã có ở `TODO.md:28`; (04) là quyết định kiến trúc, KHÔNG phải fix (sim lấy chi phí từ config có chủ ý); (05) cần Cường chốt taxonomy `track` trước | TODO (03 sev TB · 04-05 chờ quyết định) |
| D-R08 | **Đơn vị "bị NÉN" không cùng quy ước giữa các kênh** (soi đối kháng vòng 2): `accept_lift`/`rest_window`/`consult` hỏi `cadence_allows` TRƯỚC khi biết có nội dung khuyên hay không; `shift_extend` hỏi SAU. Đo seed 1000: **130/206 lần gọi (63%) và 27/58 event nén (47%) của accept_lift là "ma"** — không có lời khuyên nào để nói dù nhịp cho phép | Con số "2.670 lần bị nén" và mọi kết luận "kênh nào đói suất" (nền của D-ĐA04-03) lệch có hệ thống ~47đp giữa hai quy ước | ✅ **DONE-CODE 2026-07-29** cho `accept_lift` + `rest_window`; **CỐ Ý KHÔNG sửa** `consult`/`shift_plan` vì gate ở đó gắn với `_last_consult` ⇒ chuyển sẽ đổi `due()` tức đổi HÀNH VI. Behavior-neutral đã CHỨNG MINH bằng đo: 6.926 event khác GIỐNG HỆT. Kết quả: **50% số nén là ma** (accept_lift 93%, rest_window **100%**); số nén thật 1.377 thay vì 2.756. ⚠ Kéo theo **bác bỏ chẩn đoán "rest_window chết đói ngân sách"** của chính agent |
| D-R02 | **Thẻ chỉ HIỆN mà tài xế không tương tác có tiêu trọn một suất ngân sách không?** Sau fix Lỗi #14, `GET /advice` ghi `displayed` ⇒ mở app 7 lần ở 7 bucket khác nhau là cạn ngân sách 6/ca, dù tài xế không đọc gì | `max_proactive_per_shift` nói PROACTIVE (advisor tự đẩy) nhưng một GET do tài xế **chủ động mở app** là PULL — đếm nó vào ngân sách proactive có thể sai về ngữ nghĩa | Cần Cường chốt ranh giới PUSH vs PULL. Nếu PULL không tính: cần cờ phân biệt nguồn request (client gửi `trigger=pull|push`) và `test_showing_a_card_consumes_budget_without_any_tap` phải đổi | TODO (sev TB — **quyết định sản phẩm**, agent không tự quyết) |
| D-R11b | **Ca vắt qua nửa đêm: ngân sách/cooldown/dismiss RESET giữa ca** — `_cadence_memory` lọc `payload.date == date`; qua 00:00 thì `date` đổi ⇒ 6 suất được cấp lại và mọi dismiss mất hiệu lực, dù tài xế vẫn đang trong CÙNG một ca | Ca đêm (22:00→02:00) là ca thật của tài xế GSM; advisor sẽ nói gấp đôi ở đúng ca mà tài xế mệt nhất | Cần khái niệm `shift_id` (không phải `date`) làm khoá gom — không phải fix một dòng. Phần (a) của R-11 (`shift_end_min < SHIFT_START_MIN ⇒ +1440`) ĐÃ sửa | TODO (sev TB) |
| D-R12 | **`is_driving` không có đường nuôi từ client** ⇒ nhánh `QUEUE / unsafe_while_moving` KHÔNG BAO GIỜ chạy ở sản phẩm | Cùng họ "code tự quảng cáo nhánh không chạy" đã trả giá ở `topic_cooldown` (Lỗi #9). Lời hứa *"nudge chỉ khi không chở khách"* ghi ở `cards.js` **không được enforce ở đâu**. Ở SIM thuộc tính này được bảo đảm bằng CẤU TRÚC (vòng idle bỏ ENROUTE/ON_TRIP) ⇒ thuộc tính có ở sim, KHÔNG có ở sản phẩm — đúng loại lệch mà ĐA-04 sinh ra để diệt | Client truyền `is_driving` từ trạng thái chuyến hiện tại | ✅ **DONE-CODE 2026-07-29** — `api.advice(..., isDriving)` + `Cards.nudge` **bỏ early-return** và để luật chung quyết. An toàn KHÔNG giảm (backend trả QUEUE ⇒ silent ⇒ không vẽ card) và nay lời khuyên được **HOÃN thay vì VỨT** — đúng ngữ nghĩa QUEUE |
| D-R17 | **Ba lưới bucket cho một khái niệm "quyết định"**: dedupe suppression 20′ (`min_gap`) · `decision_id` 30′ (`DECISION_BUCKET_MIN`) · positioning 60′ (`bucket_min`) | Đo: 21/90 event `advice_suppressed` (23%) chung `decision_id` ⇒ cùng một run cho hai con số nén lệch nhau, dù code tự hứa "một lần/quyết định". ⚠ Phản biện ĐÃ BÁC hai hệ quả nặng của claim gốc: không mất reason nào (0/21 collision đa-reason) và KHÔNG ảnh hưởng mẫu số adherence (`projections.py` `continue` trước `decided += 1`) | Chọn MỘT bucket duy nhất cho cả ba — con số chọn là quyết định của Cường (20/30/60 đều có lý lẽ). Đây chính là bài học Lỗi #1 ở quy mô lớn hơn | TODO (sev THẤP — có bằng chứng, hệ quả hẹp) |
| D-R20 | **Ba test dựa vào "seed 1000 may mắn"** (`tests/test_cadence_sim.py`) | Không flaky trong CI (deterministic) nhưng **đỏ oan** khi ai đó đổi seed dù code đúng, và người sửa dễ "chữa" bằng cách nới CODE. CLAUDE.md đòi ≥5 seed cho stochastic regression | ✅ **Đã kiểm 5 seed 2026-07-29** (1000/1001/1002/2000/3160) — cả ba bất biến GIỮ: hai đơn vị adherence hội tụ (|lệch| 0,004–0,045), số lần áp tác động = số quyết định (58/58 · 44/44 · 54/54 · 57/57 · 50/50), trần ngân sách 5–6 ≤ 6. ⚠ **Nhưng seed 1001 cho 0,045 — sát ngưỡng 0,05**, đúng như cảnh báo: một seed khác có thể làm test đỏ oan và người sửa dễ nới CODE thay vì nới ngưỡng. Vẫn nên chuyển sang ngưỡng thống kê nhiều seed thay vì hằng số cứng trên một seed | TODO (sev THẤP — đã có bằng chứng 5 seed, còn lại là kỷ luật test) |
| D-R21 | **Client gửi `at_min` GIẢ** (`KIND_HOURS` = 540/840/1290) thay vì phút thật ⇒ ba wall-clock mà ĐA-04 tuyên bố xoá bỏ vẫn sống làm mốc cooldown; `recap=1290` POST lúc 12:00 đẩy `last_decided_min` vào TƯƠNG LAI ⇒ im quá mức | Phản biện đã HẠ CẤP claim gốc: kịch bản "cooldown hết hạn trước cả khi lần nói thật xảy ra" **không dựng được nữa** vì GET đã ghi `at_min = now_min` thật và reader lấy MAX. Phần dư chỉ còn im-quá-mức | ⚠ **CỐ Ý CHƯA SỬA**: sửa một phía (POST gửi phút thật, GET vẫn dùng KIND_HOURS làm bộ chọn kịch bản) tạo ra ĐÚNG loại bất đối xứng vừa sửa ở Lỗi #16/#21. Gốc là đồng hồ giả của bản demo — phải bỏ `KIND_HOURS` ở cả hai phía cùng lúc, thuộc cycle UI riêng | TODO (sev THẤP) |
