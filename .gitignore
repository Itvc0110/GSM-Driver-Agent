# draw.io temp / lock files (tạo khi mở app diagrams.net)
.$*
*.dtmp
*.bkp
~$*

# Pack nguồn đã giải nén vào repo — không commit archive
driver-income-os-ai-pack*.zip

# Claude Code worktrees (tool-generated)
.claude/worktrees/

# Secrets — TUYỆT ĐỐI không commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
*.egg-info/
build/
dist/

# Sim run outputs (artifact, không commit)
runs/

# OS / editor
Thumbs.db
.DS_Store

# Mock data — tái gen được từ seed (chỉ commit manifest + report)
data/mock/**/*.parquet
# Bản CSV để review thủ công: CÙNG dữ liệu với parquet, chỉ khác định dạng.
# Nếu không chặn thì `data/mock/realdata-v1/csv/` một mình đã là **279 MB** vào git.
data/mock/**/*.csv
# UX-CARDS (UPDATE-067): adherence log local — telemetry mock, không commit
data/ui-telemetry/
