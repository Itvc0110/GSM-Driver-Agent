# ⚠️ DEFERRED — 2026-07-20

Ba JSON Schema trong folder này (`driver_state`, `optimization_request`, `recommendation`) thuộc cách tiếp cận cũ (full optimization scaffold, D-001 trong `tracking/DEFERRED.md`). **Không dùng làm contract hiện hành** — chỉ tham khảo khi thiết kế schema mới cho scope v2 (T-011). Đọc `CLAUDE.md` và `planning/SCOPE.md` trước.
