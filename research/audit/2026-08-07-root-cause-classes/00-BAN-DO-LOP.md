# BẢN ĐỒ LỚP NGUYÊN NHÂN GỐC → KẾ HOẠCH CYCLE (2026-08-07)

> Người viết: người VẼ BẢN ĐỒ. Tôi **không** là tác giả của 5 artifact lớp, cũng không là refuter.
> Việc của tôi: trừ trùng lặp, giữ lại cái đo được, và xếp thành một kế hoạch Cường **duyệt được**.
>
> **Cách đọc nhãn tự-kiểm** trong §2: `TÔI` = tôi tự mở file:line ra đọc trong vòng này · `RF` = refuter
> độc lập đã mở ra kiểm · `AG` = mới chỉ tác giả lớp/đọc-nốt khai, chưa ai kiểm chéo.
> **Mọi con số SIM trong bản đồ này là TRÍCH LẠI**, không phải tôi đo — xem §5.

**Nguồn:** `rc-L1/L2/L3/L5/L6.json` · `pb-L-refute.json` · `mm-02/03/08/09/10-doc-not.json` ·
`00-TU-KIEM-cua-toi.md` · `c4b-do-vung-mu-tang-5.json` · `research/audit/2026-08-06-math-model-audit/00-SUMMARY.md` §3.

---

## 1. Bảng 6 lớp × verdict phản biện

| Lớp | Verdict phản biện | Thực thể MỚI-phân-biệt | Cổng chặn tái diễn khả thi |
| --- | --- | --- | --- |
| **L1** — nhiều quy ước cho một sự thật | **ĐỨNG VỮNG (MỘT_PHẦN)**. Lõi đúng, 4 thực thể nặng nhất đều trên **đường sản phẩm** và đều có **docstring tuyên bố đã hợp nhất trong khi code thì không**. Nhưng lớp **tách làm hai cơ chế con** cần hai cách chặn khác nhau, và cơ chế (a) **không thuộc riêng L1** (L3 và L6 cùng nhận) | ~9 từ `rc-L1` (khai 12, trừ #6 đã DEFERRED, #12 đã biết, #3 trùng #2) **+ 11** từ `mm-02/03/08/09/10` ⇒ **lớp ĐÔNG NHẤT** | (a) **Cổng khoá-config** (AST, khớp **đường dẫn đầy đủ**) — ĐO: 4 hit, **0 dương tính giả**, <1s ✅<br>(b) **TEST VI PHÂN hai đường trên lưới input** (mẫu có sẵn `test_range_matches_engine.py`) ✅<br>(c) ~~Cổng CANON literal~~ **BỊ BÁC BẰNG ĐO** ❌ |
| **L2** — test ghim VÔ HIỆU | **ĐỨNG VỮNG (MỘT_PHẦN)**. Artifact có **phương pháp mạnh nhất vòng**: đột biến chủ động **kèm đối chứng dương tính** (`mutant_ctrl` ⇒ 1 failed). Lớp gộp 3 cơ chế: **M1 fixture suy biến** (thật, có nguyên nhân chung đo được) · **M3 lệch đơn vị** (hiếm, nguy hiểm nhất) · **M2 vị ngữ yếu** (thói quen viết, KHÔNG có nguyên nhân chung) | **7** (khai 11, trừ 4 đã biết S2-1/S2-2/S2-4/R-2) **+ 4** từ `mm-08/09/10` | **CỔNG ĐỘT BIẾN SENTINEL** ✅ — cổng DUY NHẤT vòng này **đã chạy thật** (7 plugin, 4 phát hiện, 1 đối chứng dương, 0,3s–202s)<br>~~luật AST "parametrize trang trí"~~ **BỊ BÁC BẰNG ĐO** ❌ |
| **L3** — cơ chế MỒ CÔI | **MỘT_PHẦN — tự nhận là "một CÁI XÔ, không phải một cấu trúc"**, và tự đề nghị **KHÔNG mở cycle cho lớp**. Tôi tán thành. Phần THẬT = cơ chế (a) `Config.get` trả default im lặng — **trùng khít cơ chế (a) của L1 và "L6b" của L6** | phần (a): **3 điểm code** (đã trừ trùng) · phần (b): **9 ký hiệu** = backlog dọn dẹp · phần (c): **CÂU HỎI CHO CƯỜNG** | `test_A/B/C` khoá-config ✅ — **CHÍNH LÀ cổng L1(a)**. ⚠ Phải viết **MỘT LẦN**, không phải ba file test |
| **L4** — cổng đặt SAI VỊ TRÍ | ⚠ **KHÔNG CÓ VERDICT** — `rc-L4.json` **không tồn tại trên đĩa** và refuter không ra phán xử. Lớp này **chưa từng được truy** trong vòng | ≥**4** thực thể do người đọc-nốt tìm được **ngoài** vòng truy lớp: `B2` (mm-08-E, **kênh ĐANG SHIP**), `mm09-3`, `F-4`, `MI-2` | **CHƯA AI ĐỀ XUẤT** — đây là khoảng trống lớn nhất của vòng, và nó nằm đúng trên kênh đang ship |
| **L5** — world zero-cost | **MỘT_PHẦN, và MỆNH ĐỀ TRUNG TÂM CỦA CHARTER BỊ CHÍNH NÓ BÁC BẰNG ĐO**: *"bật giá thật thì phanh sống lại"* — SAI với `cash_cost` (bất biến **từng bit** trên [0; 4.325]đ/km; ngưỡng lật = **17–62× giá thật**). Chỉ `swap_fee` là phanh. Artifact **tự đổi tên lớp** cho đúng: *"phép so sánh MỘT-BÊN-CÓ-TIỀN + zero KHÔNG KHAI BÁO"*. Bằng chứng MẠNH NHẤT của nó (`L5-04`) refuter **xếp về L1** | **3 độc quyền** (L5-02, L5-03, L5-08) sau khi trừ 2 đã biết + 3 tự nhận thuộc lớp khác + 1 xếp sai lớp | **G2 — sổ `MONEY_PARAMS` + quét độ nhạy trên DẢI GIÁ THỰC, dán nhãn LIVE/INERT vào snapshot commit** ✅ **CỔNG THIẾT KẾ TỐT NHẤT VÒNG NÀY** vì nó assert **NHÃN KHỚP SNAPSHOT**, không assert *"phải LIVE"* ⇒ đúng kỷ luật 4, không tạo áp lực vặn tham số<br>**G1** (một tên một công thức) ✅ · **G3** (zero phải được KHAI BÁO) ✅ ⚠ G3 **đổi shape output solver** ⇒ là đổi **CONTRACT** |
| **L6** — hiệu chỉnh BÙ cho khuyết tật | ❌ **BỊ BÁC như một LỚP** (không bác các phát hiện bên trong — mọi câu trích refuter mở ra đều **đúng nguyên văn**). Bốn đường phản biện đều gãy: **4/7 thực thể trùng hoặc tự nhận thuộc lớp khác** · hai thực thể **mạnh nhất tự nhận không phải L6** · 3 cái còn lại (M3 liều · M4 giữ-khuyết-tật · M5 hằng-không-neo) **không chung cơ chế runtime nào** · **không có cổng khả thi** | **3 nợ ĐƠN LẺ** (M3, M4, M5) → vào `DEFERRED` với severity riêng, **không thành cycle** | ❌ Phần (b) "sổ hiệu chỉnh có hạn dùng" có **phản-tác-dụng do chính L6 nêu**: khi test đỏ, cách sửa rẻ là **VẶN CONFIG cho khớp phép đo — đúng chính lớp L6**. Với M3/M4, L6 tự hạ xuống *"ĐỀ XUẤT QUY TRÌNH, không phải test"* |

### 1b. Kết luận về giả thuyết lớp

**LỚP LÀ MỘT CÁCH KỂ CHUYỆN GỌN CHO BA CƠ CHẾ THẬT, không phải sáu cấu trúc.**
Ba cơ chế — **và chỉ ba** — vượt cả bốn phép thử (chung cơ chế runtime · không trùng lớp khác ·
thực thể mở `file:line` ra kiểm được · cổng khả thi và **tự chứng minh đỏ được**):

| # | Cơ chế | Bị mấy lớp cùng nhận | Cổng |
| --- | --- | --- | --- |
| **(1)** | **KHOÁ CONFIG MA + default IM LẶNG** — 3 điểm code (`advice_bridge.py:202`, `:209`, `world.py:172`) | **BA** (L1(a) = L3(a) = L6b) ⇒ **ba đề xuất cổng TRÙNG NHAU** — bằng chứng mạnh nhất rằng phân lớp hiện tại **không phải một phân hoạch** | cổng tĩnh, 4 hit, 0 FP |
| **(2)** | **CHÉP LUẬT / HAI CÔNG THỨC CHO MỘT KHÁI NIỆM** trên đường sản phẩm | L1(b), + `L5-04` refuter chuyển về đây | **TEST VI PHÂN**, **KHÔNG** phải quét literal (ĐO: 165 bắn/50 file) |
| **(3)** | **TEST KHÔNG PHÂN BIỆT ĐƯỢC ĐÚNG VỚI SAI** | L2 (M1+M3) | **CỔNG ĐỘT BIẾN SENTINEL** |

⚠ **Hai con số phải nói ra để không ai xếp ưu tiên theo "lớp nào tìm được nhiều hơn":**
54 suất thực thể được khai → **~36 phân biệt** (thổi ~50%). Ba dòng code bị đếm **2–3 lần**.
`advice_bridge.py:202` một mình được tính **ba suất**.

⚠ **Một điều kiện tiên quyết KHÔNG thuộc lớp nào:** `00-TU-KIEM` §3 đo được **6/6 kênh advisor đang TẮT**
(`pilot_dongda.yaml:330-343`) ⇒ **bán kính ảnh hưởng HÔM NAY** của phần lớn nợ là **0**.
**NHƯNG câu đó ĐÚNG NHƯNG KHÔNG ĐỦ** — có hai đường vòng đo được: (a) `demo_session.py:68-71` **bật lại**
`shift_plan`/`accept_lift` cho Track UI; (b) S2 **đã đi dây đầy đủ** trên backend sản phẩm, cổng thật là
`missing_state` **chứ không phải ĐA-07** (`advice_checkpoint.py:206-209`, router mount `main.py:44` — tôi tự kiểm).

### 1c. Lớp thứ 7 — BA artifact ĐỘC LẬP cùng đề nghị, tôi KHÔNG chốt

`mm-03` gọi là **"PROXY SAI ĐẠI LƯỢNG"**, `mm-08` gọi là **"L7 — objective thiếu vế"**, `mm-10` gọi là
**"PROXY THAY ĐẠI LƯỢNG ĐỊNH GIÁ"**. Nó phủ **4/6**, **4/9**, **3/8** phần *"KHÔNG thuộc lớp nào"* của ba artifact.
Nếu đúng, bảng 6 lớp **đang bỏ sót khuôn LỚN NHẤT rồi gọi phần còn lại là "nợ độc lập"**.
**Vì sao KHÔNG gộp vào L1:** L1 = một tầng **TỰ TÍNH LẠI** cái engine **đã sở hữu**; ở đây **KHÔNG tầng nào
sở hữu đại lượng đúng cả** — không ai tính sai, mà là **không ai có**. Hai cổng chặn **khác hẳn nhau**.
⇒ Tôi ghi lại như **giả thuyết chưa truy**, không đưa vào kế hoạch (chưa ai grep-driven nó như L1..L6).

---

## 2. Danh sách thực thể MỚI (chưa có trong `tracking/DEFERRED.md`)

**Tổng: 76 thực thể.** ⚠ Con số này là phép đếm **của tôi trên bảng tôi dựng** và phụ thuộc chỗ tôi kẻ
ranh giới *"một thực thể"* — refuter tự ước ~36 chỉ trên 5 file `rc-L*` và ghi *"ai dùng phải đếm lại"*.

### 🔴 A. ĐƯỜNG SẢN PHẨM — nặng nhất (12)

| ID | file:line | Hậu quả | Sev | Tự kiểm |
| --- | --- | --- | --- | --- |
| **A1** | `ui/backend/app/adapters/advisor.py:321-327` + `:389-399` | `reason_code` và `_infeasible_text` **first-match**: khi hụt giờ VÀ tỷ lệ nhận cùng dưới ngưỡng, card **chỉ nói "thiếu giờ"** và khuyên *"đừng cố quá"*, giấu đúng vế cứu được 30–60k thưởng ĐÃ KIẾM. Solver ghép ĐỦ lý do (`bonus_feasibility.py:111-116,180-190`), tầng sản phẩm dựng lại **mất thông tin**. ĐO: **549 ca / 17,58 triệu đ** trên mock, t=20:00 | **CAO** | **TÔI** (đọc code) + AG (549 ca) |
| **A2** | `ui/backend/app/routers/advice.py:126` (`_phase_of` dùng hằng `SHIFT_START_MIN=360`) vs `:185` (`get_advice` lấy từ query) | Hai công thức PHA CA trong CÙNG một request. Docstring `:113` tuyên bố *"MỘT công thức duy nhất"* — **nhãn SAI**: F4 bỏ được neo KẾT ca cứng nhưng để nguyên neo MỞ ca. ⇒ với **mọi tài xế không mở ca đúng 06:00**, nút **"Bỏ qua"** hoặc vô tác dụng hoặc làm advisor im ở pha chưa hề tắt. ĐO: ca đêm lệch **4/4** mốc | **CAO** | **TÔI** + RF |
| **A3** | `src/gsm_core/lifecycle/checkpoint.py:346` (`>= 6`) và `:351` (`20*60`) | Đường sản phẩm v2 **chép lại luật nhịp** với hằng riêng; canonical là số DẪN XUẤT `cadence.effective_gap_min = max(20, 30) = 30′` (`cadence.py:105`). Cửa sổ lệch **+20..+29′ = 33% bucket quyết định** ⇒ card ĐẾN TAY tài xế nhưng `event_id` trùng khoá bucket ⇒ store dedupe ⇒ **(a)** trần 6 thẻ/ca mất hiệu lực, **(b)** mẫu số event-adherence hụt. **Một con số adherence sản phẩm bị thổi/hụt bởi CƠ CHẾ, không bởi hành vi tài xế** | **CAO** | **TÔI** + RF |
| **A4** | `ui/backend/app/services/advice_checkpoint.py:569-584` + `src/gsm_core/lifecycle/checkpoint_store.py:302-309` | `_cadence_memory` duyệt **TOÀN BỘ** checkpoint của tài xế (SQL `SELECT ... ORDER BY rowid`, chỉ lọc `driver_id`) ⇒ `proactive_count = len(offered_ids)` **cộng dồn TRỌN ĐỜI** ⇒ sau **6 thẻ đầu tiên trong lịch sử tài khoản**, v2 im **vĩnh viễn** với `shift_budget_exhausted`; `dismissed_topics` là **set không gắn pha/ngày** ⇒ một lần bấm Ẩn tắt topic **vĩnh viễn**. **0 test** chạm nó | **CAO** | **TÔI** (đọc SQL + hàm) — hậu quả **chưa reproduce end-to-end** |
| **A5** | `ui/backend/app/adapters/advisor.py:116,120,145-159` | Hai thước cho `acceptance_rate`: UI nuôi S1 bằng **shrunk 7-ngày as-of** (đứng im cả ngày) trong khi gate chính sách áp **tỷ lệ của CHÍNH ngày đó**. ĐO toàn mock: 12.373 cặp (tài xế, ngày); verdict qua/không-qua vách 0,85 **LẬT 2.413 = 19,5%**, trong đó **1.430 theo chiều NGUY HIỂM** (UI im vì tưởng trên ngưỡng, thực tế dưới ⇒ mất thưởng) | **CAO** | AG (`mm-03` tự kiểm code + đo) |
| **A6** | `src/gsm_core/solvers/bonus_feasibility.py:20` (`DEFAULT_TRIPS_PER_HOUR=1.5`) → `_hour_rate`, mẫu số `hours_budget_remaining=(shift_end−now)/60` | Hằng đi qua **RANH GIỚI MẪU SỐ bằng NHÃN**: 1,5 đúng ở mẫu số `market_state` (IDLE+enroute) nhưng `bonus_feasibility` đi theo **giờ ĐỒNG HỒ** (đo 1,2711, 8 seed) ⇒ fallback **ước CAO +18,0%**. Fallback chỉ chạy khi **thiếu lịch sử** — tức **đầu ca / tân binh**, đúng lúc câu *"còn kịp mốc không"* quan trọng nhất | **CAO** | AG (L6, 8 seed) — ⚠ vế *"config 1,5 sai 24%"* của `rc-L1` **ĐÃ BỊ BÁC** (sai mẫu số) |
| **A7** | `src/gsm_core/solvers/bonus_feasibility.py:130-145` (nhánh `already_maxed` return sớm, `:141 "sensitivity": []`) | Khối cliff nằm **mãi `:213-218`, SAU return** ⇒ không đời nào chạy trên nhánh maxed ⇒ người treo **170k sát vách 0,85** nhận *"không có gì cần chỉnh. Giữ nhịp hiện tại"*. **Cùng return sớm này ĐÃ TỪNG đánh rơi `constraints`** và phải vá bằng C2/UPDATE-076 — một hàm, hai lần, cùng cơ chế | TB | AG (tái lập bằng input tổng hợp). ⚠ **PHƠI NHIỄM HÔM NAY = 0**: 39 ca `already_maxed` trên mock, acceptance 0,923–0,975, **không ca nào** rơi vào [0,85; 0,88) |
| **A8** | `ui/backend/app/services/demo_session.py:487` (`haversine/20.0*60`) vs `ui/backend/app/routers/routing.py:216` (`dist*2.5`) vs `pilot_dongda.yaml speed_kmh` (17/25/30) | **BA quy ước** cho thời gian đi hết một đoạn thẳng, hai cái nằm cạnh nhau trên cùng đường demo và không đồng ý (ĐO: 16′ vs 13′ trên 5,3 km = lệch **23%**). Nặng hơn: `routing.py:46` bơm distance **haversine THÔ** thẳng vào `quote_distance` ⇒ cước tính trên 5,3 km thay vì ~7,7 km lộ trình ⇒ **payout báo THIẾU 27,5%**. Có nhãn `route_is_real_road:false` cho ĐƯỜNG, **con SỐ TIỀN không mang nhãn "ước non"** (§5) | **CAO** | RF + AG. ⚠ 27,5% chỉ đúng cho đoạn 5,3 km (có `base_fare/base_km`) |
| **A9** | `src/gsm_core/advisor/checkpoint_templates.py:29` và `:31` | `required_fact_ids` / `required_caveat_ids` có **ĐÚNG 1 ref = dòng khai**, trong khi anh em `required_number_ids` SỐNG (producer `:96` + test). Đây chính là chỗ để **MỘT CỔNG** kiểm *"thẻ có mang đủ caveat/fact chưa"* (§5 bắt buộc) — chúng trơ nên không cổng nào kiểm được, và người đọc module tin rằng có | TB | **RF** |
| **A10** | `ui/contracts/advice.json:37` (`charge_now`, `demand_shift`) và `:25` (`insufficient_history`) · `ui/contracts/driver_state.json:18` (`BREAK`) | 4 giá trị enum **0 producer** trên toàn repo (grep hết `.py/.dart/.ts/.tsx/.html`). `insufficient_history` là **lý do im lặng PHỔ BIẾN NHẤT** mà tài xế mới sẽ gặp và **không có đường phát**. Client của Khánh có thể code nhánh cho chúng và nhánh đó chết. ⚠ Chính `advice.json:25` **đã ghi bài học** *"giữ một mã không đường phát chính là mẫu khai-mà-không-có-đường-chạy mà cycle này đi bịt"* — rồi vẫn còn ba mã | TB | AG (grep đầy đủ) |
| **A11** | `ui/backend/app/routers/advice.py:129` (`_cadence_memory(..., phase)`) | Tham số `phase` **không được đọc** (AST-proved): chữ ký nói ký ức nhịp có **phạm vi theo PHA**, thân hàm dựng ký ức cho **CẢ NGÀY**. Mảnh còn sót của chính lần sửa F4 ở A2 | TB | **RF** |
| **A12** | `ui/backend/app/services/advice_checkpoint.py:39,206-209,213-225` + `ui/backend/app/main.py:44` | **S2 (`shift_dp`) ĐÃ ĐI DÂY ĐẦY ĐỦ** trên backend sản phẩm (validate schema → artifact → candidate → presenter); thứ duy nhất chặn là **thiếu `RuntimeStateProvider`** (`missing_state`), **KHÔNG phải ĐA-07**. Và ĐO trên **shape sản phẩm** (l1r, 2026-08-15 & 2026-09-28): `eo==0` **0,0%**, cap 2,4 **BIND 88,6%** ⇒ **`S2-3` SỐNG NGUYÊN trên đường sản phẩm**, không chỉ trong sim | **CAO** (cho cách đọc kế hoạch) | **TÔI** (đọc `:206-209` + `main.py:44`) + AG (88,6%) |

### 🟠 B. KÊNH SIM ĐANG SHIP — positioning / S4 (10)

> Kênh **DUY NHẤT** Cường đã duyệt bật mặc định (`positioning_overrides: wait_only`, PASS 9/9 ĐA-08).

| ID | file:line | Hậu quả | Sev | Tự kiểm |
| --- | --- | --- | --- | --- |
| **B1** | `src/gsm_core/features/market_state.py:81` (`slots = int(demand // trips_per_driver_per_bucket)`) vs `pilot_dongda.yaml:120,142,147` (`candidate_ring_k` 4, `k_max` 6 ≈ 2,22 km, `eta_max_min` 11) | **Hai quy ước cho "ai phục vụ được cầu này"**: dispatcher SỞ HỮU kernel phục vụ rộng hàng chục ô; `market_state` tự tính trần trên **MỘT ô res-9 (~0,35 km)** với `tphe=1,5`. ĐO: **87,6–88,2%** cell-bucket có `slots=0`; trần toàn lưới **tối đa 3**; `cap_left` của ô nhận gán ∈{1,2}, **96%+ là 1**. **ĐÂY LÀ RÀNG BUỘC CHI PHỐI TOÀN KÊNH** — nó quyết định LIỀU (62–83 lượt gán/ngày cho 90 tài xế) | **CAO** | **TÔI** (đọc `:81-82`) + AG (histogram, 3 seed) |
| **B2** | `src/gsm_sim/world.py:475-477` (slot tiêu) vs `:495` (`standby_follow_draw` — coin adherence) | **Cổng ĐÚNG NỘI DUNG đặt SAU bước gán** (L4): slot đã bị Hungarian tiêu **trước khi** biết người đó có đi không. ĐO: assigned 78/83/62 vs coin-follow 38/47/33 ⇒ **đốt 51,3% / 43,4% / 46,8%**. Ghép với `cap_left`=1 (96%+): **MỘT lượt đốt = XOÁ TRẮNG ngân sách lời khuyên của ô đó trong bucket đó**. Cùng khuôn `B2`-cooldown và `D-M3-20` | **CAO** | **TÔI** (đọc thứ tự `:475→:495`) + AG (3 seed) |
| **B3** | ĐO 3 seed arm B3w coverage=all vs `tracking/updates/UPDATE-087-*.md:15` | **LIỀU THẬT của kênh đang ship**: chỉ **32,2–44,4%** đội hình thực sự di chuyển ≥1 lần/ngày; được gán ≥1 lần = 53,3–63,3%. **`+6.016đ/người/ngày` là trung bình TOÀN ĐỘI** mà ~2/3 số người **không nhận can thiệp nào**. Hai cách đọc (hiệu ứng tập trung ở ~35% người di chuyển **vs** lan toả hệ thống) dẫn tới **hai quyết định ship KHÁC HẲN NHAU**, và chưa ai phân rã | **CAO** (rủi ro đọc sai kết luận **đang ship**) | AG (3 seed) — phân rã payout **CHƯA ĐO** |
| **B4** | `advice_bridge.py:298-315` (`share`) + `market_state.py:81-82` | Liều **BÃO HOÀ theo TRẦN chứ không theo ĐỘ PHỦ**: giảm phủ **4×** chỉ mất ~**45–55%** liều (executed 37/45/31 → 21/20/14) vì ứng viên luôn dư **2,6–3,3×** so với trần. ⇒ caveat *"adoption 100%"* của UPDATE-087:66 **đúng chiều nhưng SAI ĐỘ DỐC**; ai chiết khấu +6.016đ **tuyến tính** theo độ phủ pilot đang chiết khấu **quá tay** | TB | AG (3 seed) — ⚠ mới đo **LIỀU**, **CHƯA đo PAYOUT** theo độ phủ; **không được suy từ liều ra tiền** (kỷ luật 4) |
| **B5** | `src/gsm_core/solvers/capacity_alloc.py:101-109` (`max(1, int(round(v*0.8)))`) + `tests/test_capacity_alloc.py:126-132` | Sensitivity *"capacity −20%"* **KHÔNG BAO GIỜ nhúc nhích** ở điểm vận hành thật (v=1 → `max(1,1)=1`; v=2 → `round(1,6)=2`). ĐO **0/48 lô**. Test ghim nó chỉ assert **khoá TỒN TẠI**, trên fixture `capacity=3` **ngoài điểm vận hành** (thật là {1,2}). Solver báo một "phân tích độ nhạy" mà **theo cấu trúc không thể khác baseline**, và test bảo vệ nó **cũng không thể đỏ** | TB | AG |
| **B6** | `src/gsm_core/solvers/capacity_alloc.py:91` (`herding_avoided = n_stag + len(unassigned)`) → `:111-113` digest | **76,1–79,8%** của chỉ số "chống dồn cung" là `unassigned` = **người KHÔNG được khuyên gì** (đứng nguyên = y hệt arm đối chứng). Không lật kết luận nào (không vào ĐA-08), nhưng ai trích *"chống dồn cung 178–211 lượt/ngày"* làm bằng chứng kênh hiệu quả thì đó là **vòng tròn** | TB | AG |
| **B7** | `src/gsm_core/solvers/capacity_alloc.py:45` (`np.full(LARGE)`) → `:46-50` (vòng đôi **ghi đè MỌI ô**) → `:56` (`if cost >= LARGE: continue`) · `:19` `LARGE=1e6` · `:25` `src_label` | **Nhánh LARGE là CODE CHẾT** (fuzz 200 input: không lần nào kích hoạt) ⇒ **S4 không có cách biểu đạt "slot này CẤM với người này"** — đúng chỗ cần cho ràng buộc PIN. `src_label` xuất hiện **đúng 1 lần = trong signature** | TB | AG (đọc + fuzz) |
| **B8** | `src/gsm_sim/world.py:443-446` (SOC chỉ vào `priority_soc`) + `capacity_alloc.py:48` (`pen ∈[0,1]` vs mismatch `+10`) | Có lượt gán cho tài xế **SOC 8,7% / 20,3% / 22,2%** trong khi ngưỡng đổi pin là 20%. SOC là **XẾP HẠNG MỀM, không có SÀN**, và `pen` là 1/10 mức lệch nên **SOC không bao giờ thắng được mismatch** ⇒ slot khan (=1) tiêu cho người gần như chắc chắn đi đổi pin | TB | AG (conf TB — chỉ đo min theo seed) |
| **B9** | `src/gsm_core/solvers/capacity_alloc.py:77` (`def solve(ai, params=None)`) | `params` **không xuất hiện một lần nào** trong thân (AST-proved); caller sản phẩm duy nhất `world.py:454` cũng không truyền. **Kênh Cường duyệt bật mặc định trưng ra một cửa tham số hoá mà nó không có** ⇒ ai A/B kênh vị trí qua `params` đo Δ=0 và kết luận *"tham số không ảnh hưởng"* — đúng cái mà docstring `test_config_flags_wired.py` gọi là *"loại kết luận sai tệ nhất"*. Ngưỡng thật cứng trong module (`STANDBY_SAFETY_FLAGS`, sensitivity −20% hardcode `:103-110`) | TB→**CAO** cho độ tin phép đo | **RF** |
| **B10** | `src/gsm_core/features/market_state.py:86` (`sd = demand/eff`) và `:99-103` (ranked theo −sd_ratio) | **Thứ tự `ranked_cells` KHÔNG CÓ CONSUMER SỐNG NÀO**: bị vứt **HAI LẦN** — `world.py:442` dùng làm TẬP cho min-theo-khoảng-cách, rồi `capacity_alloc.py:34` `sorted()` lại theo **alphabet**. Hôm nay moot, nhưng thành load-bearing **NGAY khi ai sửa B1/D-ADV-01** ⇒ ai sửa phải sửa `:86` sang `λ/(eff+1)` cùng lúc | THẤP | AG (grep đầy đủ) |

### 🟡 C. ĐỘ TIN CỦA PHÉP ĐO — gác mọi quyết định tương lai (13)

| ID | file:line | Hậu quả | Sev | Tự kiểm |
| --- | --- | --- | --- | --- |
| **C1** | `src/gsm_sim/parallel.py:420` (`_ONE_WAY_PREFIXES`) vs `:557-564` (`keys` **chép cứng**) vs `src/gsm_sim/sim_metrics.py:535` (`for r in REST_RAILS`) | **7 khoá gắn nhãn *"cổng một chiều canh"* mà cổng KHÔNG hề soi** (`xveto_soc_low_n`, `xveto_fatigued_n`, `xveto_would_exceed_fatigue_n`, `commit_made/kept/broken/cleared_n`) và **9 khoá vắng khỏi `a_mean`** (7 cái trên + `xveto_calls_n`, `xveto_fired_n`). `xveto_calls_n` là **MẪU SỐ** của kênh kéo ca — vắng nó **vi phạm đúng nguyên tắc mà `parallel.py:561-564` tự phát biểu**. Artifact vẫn in `"one_way_gate": "sim_metrics.health_guardrail_flags (D-M3-05)"` — **một lời khai quản trị KHÔNG CÓ THẬT**. **LỖI TÁI DIỄN**: `parallel.py:415-419` tự chép rằng danh sách *"đã HỞ hai lần"* | **CAO** — **điều kiện tiên quyết của mọi cycle đo kênh ngủ** | **TÔI** (probe `c4b-do-vung-mu-tang-5.py` tiêm từng khoá + `inspect`) |
| **C2** | `scripts/measure_e10.py:468` và `scripts/measure_l104.py:52` (`mean(a.payout_vnd)`) vs `src/gsm_sim/parallel.py:132,141` + `sim_metrics.py:156-161` (`net = payout − cost_vnd`) | **HAI công thức cho CÙNG khoá `net_mean_all`**, trùng nhau **CHỈ VÌ `cost_vnd ≡ 0`**. ĐO 5 seed với giá thật (150đ/km + 9.000đ/lượt): lệch **23.685–25.603đ/người/ngày = 9,8–10,3% payout** — **GẤP 4–6 LẦN** hiệu ứng advisor từng được báo (+6.016đ). `tests/test_net_metric.py` **chỉ canh đường `parallel`**, không canh đường `scripts` | **CAO** | **RF** (đọc code) + AG (5 seed) |
| **C3** | `tracking/QUYET-DINH-2026-07-30-nam-diem.md:95` + `PLAN-2026-07-30-...:65-68` + `UPDATE-107:51-55` | **Cổng QUYẾT ĐỊNH của chương trình** (`Δ net_mean_all > 0`, CI95 không trùm 0, n≥100) đặt trên một chỉ tiêu **TÊN LÀ NET nhưng ĐO GROSS**. Mọi phán quyết PASS/NO-GO đã chạy qua nó thực chất là phán quyết về **payout gộp**. ĐẾM: **20 file** dùng chữ `net_mean`, **13 KHÔNG có caveat** | TB (hôm nay vô hại) | AG |
| **C4** | `scripts/measure_e10.py:226` (`underfed ⇔ n_idle ≥ 2 ∧ λ < n_idle × tphe`) vs `src/gsm_core/features/market_state.py:81-82` | **Định nghĩa THỨ BA** về "ô thiếu cầu": bỏ `floor`, bỏ `supply_incoming`, đổi ngưỡng. ⇒ precision/recall của trigger `wait` — **con số dùng để CHỌN `positioning_trigger`** — đo so với một thước riêng của script, **không phải luật engine thi hành**. ⚠ `gsm_sim/market_state.py:5` nói thẳng *"Cố ý không tính lại trần/tỷ lệ ở đây — hai chỗ cùng tính một luật chính là mẫu lỗi T-046"* | TB | AG — độ lệch **CHƯA lượng hoá** |
| **C5** | `tests/test_shift_dp.py:126-133` (`test_sensitivity_demand`) | Comment khai tính chất **ĐƠN ĐIỆU**; assert thật sự chỉ là `all(v is not None)`. Cộng fixture suy biến `_spi(buckets=6)` ⇒ `_required_rest=0` ⇒ **`delta_payout ≡ 0.0` trên 120/120 cấu hình**. **ĐỘT BIẾN**: thay MỌI trường số sensitivity bằng −999999.0 ⇒ **19 passed** (đối chứng dương `mutant_ctrl` ⇒ 1 failed). Rộng hơn: **11 file test solver, 208 passed** dưới đột biến ⇒ **toàn bộ vế SỐ của lời cam kết "luôn nêu bất định" (§5) hiện KHÔNG có test nào canh** | **CAO** | AG (**đột biến chủ động + đối chứng dương tính** — phương pháp mạnh nhất vòng) |
| **C6** | `tests/test_shift_dp.py:81` (`assert projected_bonus_tier >= 160`) | **LỆCH ĐƠN VỊ**: trường được khai `_num(proj_tier, "vnd")` (`shift_dp.py:380,387`), còn 160 là **NGƯỠNG ĐIỂM**. Fixture `day_bonus_tiers` mốc thấp nhất = **30.000đ** ⇒ assert thoả ở **MỌI** mốc ⇒ nó **không kiểm được "đạt mốc 160"**. **Thực thể CHẮC NHẤT của cả năm artifact** — kiểm được bằng chính fixture | **CAO** | **RF** (đọc nguyên văn fixture + assert) |
| **C7** | `tests/test_shift_dp.py:75-81` (`test_terminal_bonus_drives_online`, `assert n_online >= 3`) | ĐO trên đúng trục test khai là đang kiểm: `n_online = 6` (**TOÀN BỘ** 6 bucket) ở `points_now` ∈ {0,10,40,60,100,140,190,200,210} — kể cả khi mốc **BẤT KHẢ** và khi tài xế **ĐÃ MAX**. Kết hợp C5+C6: **KHÔNG assert nào trong `test_shift_dp.py` phân biệt được DP-có-nhìn-mốc với DP-mù-mốc** ⇒ đây là lý do một nợ CAO (`S2-2`/ADV-01) sống được qua nhiều vòng | **CAO** | AG |
| **C8** | `tests/test_fairness_metrics.py:79-85` | `expired_n` là **TẦNG KHÁCH HÀNG** của guardrail 5 tầng, dùng cho mọi phép A/B — nhưng ba vị ngữ đều không ràng buộc gì (`>= 0` tautology theo kiểu; **oracle TỰ QUY CHIẾU** `expired_rate == expired_n/max(1,total)`). ĐỘT BIẾN đặt `expired_n:=0` ⇒ **16 passed**; giá trị thật seed 1000 = **206/1210 = 17,0%**. ⚠ **CÙNG FILE** `:55-66` đã có lan can *"không được im lặng bằng 0"* cho `supply_cell_hhi` — **không có lan can tương đương cho `expired`** | TB-CAO | AG (đột biến) |
| **C9** | `tests/test_m0_integrity.py:212` (`result.config.get("dispatcher.offer_cooldown_min", 10.0)`) | Test canh bất biến M0-2 đọc **CÙNG khoá MA** với **CÙNG default** như `world.py:172` ⇒ nó so hành vi engine với **BẢN SAO HẰNG CỦA CHÍNH NÓ**, không với cấu hình. Ai đổi 10.0 ở engine mà quên test ⇒ test vẫn XANH. (Nửa `world.py:172` = **D-SIM-K6 đã DEFERRED**; chỉ nửa test là MỚI) | TB | **RF** |
| **C10** | `tests/test_mission_knapsack.py:79-86` (`<= hours + SLOT_MIN/60`) và `:54-63` (`_brute_force`) | **Hai cơ chế vô hiệu trong cùng một file**: (a) dung sai `+0,25h` **bằng đúng độ hạt** ⇒ bất đẳng thức luôn đúng ⇒ **test hợp thức hoá đúng khuyết tật nó phải bắt**; (b) oracle brute-force **tái sử dụng công thức cost của chính solver** ⇒ 30 seed parametrize chỉ kiểm DP-vs-enumeration, **không thể bắt sai MÔ HÌNH** | THẤP (S6 mồ côi) | AG |
| **C11** | `tests/test_future_leak_gate.py:82-92` (`_cases()` liệt kê **7 deriver viết TAY**) | Họ deriver có **8**; `derive_session_summary_input_l1r` (`from_l1r.py:201`) là **deriver DUY NHẤT không nhận `t_now`** và **nằm NGOÀI cổng**. ⇒ **mỗi thành viên mới của họ mặc định NGOÀI cổng, im lặng**. **Cơ chế RIÊNG**: không phải "fixture suy biến" mà là **"cổng thường trực dùng DANH SÁCH TAY thay vì quét cả họ"**. **Cổng RẺ NHẤT vòng**: đổi sang quét theo mẫu tên `derive_*_input_l1r` | TB | AG (grep xác nhận 0 hit) |
| **C12** | `src/gsm_core/mockgen/verify_stats.py:87-88` (`if not inb and k not in ('payout_ft_vnd','trips_ft_median'): ok = False`) | **2/4 chỉ số được MIỄN làm đỏ**, báo cáo vẫn in *"KẾT LUẬN: PASS"* ⇒ người đọc `ROUND-2-stats-report.md` kết luận mock đạt hiện thực trong khi một nửa chỉ số nằm ngoài dải benchmark. Ghi công bằng: file này **TRUNG THỰC ở chỗ khác** (in `**GAP** (ghi T-021, không che)` từng dòng) — lỗi **chỉ ở BOOLEAN tổng** | TB | **RF** (đọc nguyên văn) |
| **C13** | `ui/backend/tests/test_range_matches_engine.py:168-175` | Đây là **CỔNG của `D-M3-17`** (nợ sinh ra vì UI báo tầm pin lệch engine **1,76×**). Tham số hoá `soc ∈ {30,55,95}` nhưng **không gọi mockdata lần nào**; assert 2 **không chứa `soc`** ⇒ ba ca cho ba assert **byte-giống nhau**. Toàn file neo **ĐÚNG MỘT điểm** (`bikes[0]`, soc=60) trong khi catalog có **110 tài xế bike**. ĐỘT BIẾN kẹp `range ≤ 40 km` cho **109/110** tài xế ⇒ `ui/backend/tests` **205 passed** | **CAO** | AG (đột biến) — ⚠ RF **BÁC** cách bắt bằng AST (xem §4.4) |

### 🟢 D. KÊNH NGỦ / SOLVER MỒ CÔI / SIM (36)

> Bán kính **hôm nay = 0** với hầu hết. Vào `DEFERRED` **kèm điều kiện mở lại**, trừ 2 cái **ĐÍNH CHÍNH
> một điều kiện mở lại đã ghi SAI** (D1, D2) — hai cái đó phải sửa hồ sơ **ngay**.

| ID | file:line | Hậu quả | Sev | Tự kiểm |
| --- | --- | --- | --- | --- |
| **D1** | `src/gsm_core/solvers/shift_dp.py:245-249` (REST vô điều kiện, reward 0) vs `:250-254` (END, `if v > best_v` **CHẶT**, xét **SAU**) | **END BẤT KHẢ ở MỌI mức chi phí** — chứng minh dominance **qua REST** (độc lập chi phí), ĐO **0/225** case ở **7 mức**, kể cả `cash=1.000.000đ/km + fee=1.000.000đ`. ⇒ **điều kiện reopen của `S2-5`/`mm-02#1`** (*"chỉ có nghĩa khi policy costs ACTIVE, as_of ≥ 2029-04"*) là **VÔ HIỆU**, và guard `mm-02` đề xuất (cảnh báo khi `cash==0 ∧ fee==0`) là **guard ÂM TÍNH GIẢ**. Cũng **BÁC** thành viên charter `E-05` khỏi L5 | **CAO** (cho hồ sơ) | AG (probe A, 225 case) |
| **D2** | `src/gsm_core/solvers/shift_dp.py:193-203` (`cash_km` **chỉ** vào nhánh ONLINE) | `cash` là **phép co giãn đơn điệu trên phần thưởng của ONLINE** ⇒ argmax **KHÔNG THỂ đổi** cho tới khi `online_net` đổi DẤU. ĐO: **bất biến TỪNG BIT** trên [0; 4.325]đ/km, 36 case; ngưỡng lật = `ppo/avg_dist` = **4.325đ/km** = **17–62× giá thật**. ⇒ **BÁC điều kiện mở lại của `D-E4-01`** và đề xuất sweep `{0,70,150,250}` ở `PLAN-cycle-wx-2026-07-29.md:108`: **cả 4 mức cho kết quả Y HỆT** | **CAO** (cho kế hoạch) | AG (36 case × 9 mức) |
| **D3** | `src/gsm_core/solvers/shift_dp.py:430-431`; `n_swaps` **KHÔNG có** trong `solution` (`:423-432`) | `expected_swap_cost_vnd = n_swaps × fee` với `fee=0` ⇒ lịch **7 bucket SWAP** báo `0.0đ`, **giống hệt** lịch 0 SWAP; consumer **không thể tự định giá lại** vì `n_swaps` không expose. ⇒ khẳng định ở `UPDATE-097:15` (*"consumer đủ số để tính net công bằng cho CẢ HAI lịch"*) là **SAI dưới config ship** | TB | **RF** |
| **D4** | `src/gsm_core/solvers/shift_dp.py:332` (`policy_costs_as_of`) — **0 call site production** | Cơ chế **DUY NHẤT** tuyên bố *"số hạng tiền này đang TẮT / UNKNOWN"* chỉ bật khi caller truyền `as_of`; không ai có động cơ truyền ⇒ mọi report S2 **không mang `terms_active`, không mang caveat chi phí**. Dưới §5 đây là **khai báo bắt buộc bị bỏ qua theo MẶC ĐỊNH chứ không theo QUYẾT ĐỊNH** | TB | AG |
| **D5** | `bonus_feasibility.py:225-244` → `ui/backend/app/adapters/advisor.py:279-312` | Hàm mục tiêu S1 có **PHẦN THƯỞNG bằng tiền** và **CHI PHÍ bằng GIỜ**, **không hề có số hạng tiền cho giờ đó**. Thẻ **SẢN PHẨM** nói *"còn với được mốc {tier_vnd}… khoảng {hours_needed} giờ chạy nữa"* ⇒ phép *"đáng hay không đáng"* **không tính được** bằng số hiện có | TB (**VẮNG MẶT CẤU TRÚC**, chưa số nào sai) | AG — độ lớn ~1.500đ là **DERIVED, CHƯA ĐO** |
| **D6** | `tests/test_c1_cost_term.py:88-96` (cash=100đ/km, **ASSERT lịch KHÔNG đổi**) vs `:104-106` (5.000đ/km) và `tests/test_f098_defects.py:70,95` (4.327đ/km) | Bộ test của chính số hạng chi phí **biến sự suy biến thành hành vi KỲ VỌNG**: nó khẳng định ở **mức giá THỰC** lịch không đổi, và chỉ chứng minh số hạng "cắn" ở **20–62× thực tế**, **vừa đúng trên ngưỡng lật 4.325** của D2 ⇒ suite xanh **vĩnh viễn** dù số hạng trơ trong **toàn dải giá thực** | TB | AG |
| **D7** | `configs/pilot_dongda.yaml:185-207` (`idle_impatience_step_min 20 / max_steps 2`); defect tại `src/gsm_sim/world.py:1146-1175` | **LIỀU của thuốc giảm đau chọn theo MỤC TIÊU ĐO LƯỜNG CỦA THÍ NGHIỆM**: config tự khai *"served chỉ +0.023 so với tắt (ngưỡng TỰ ĐẶT ≤ +0.05) để KHÔNG ĂN MẤT DƯ ĐỊA CỦA ADVISOR"*. Defect thật (`_actor_demand_hint` là prior cache, **không bao giờ cập nhật từ trải nghiệm**) vẫn nguyên. ⇒ **tham số thế giới và biến kết cục của thí nghiệm bị GHÉP**; `+6.016đ` là một hàm của liều mà **chưa ai công bố đạo hàm** | TB→**CAO** cho cách diễn giải | **RF** (trích nguyên văn) — độ lớn **CHƯA ĐO** |
| **D8** | `src/gsm_sim/archetypes.py:35` · `pilot_dongda.yaml:232-237` · `mockgen/verify_stats.py:20-22,92-94` · `tests/test_sim_realism.py:167-172` | **L6 ĐẢO CHIỀU**: khuyết tật được **GIỮ LẠI CÓ CHỦ Ý** vì nó sinh ra "dư địa advisor" — 4 chỗ độc lập gọi một sai lệch hiện thực bằng tên đó; **P4 chiếm 18% đội**; và **nay có một TEST CẤM thu hẹp khuyết tật**. ⇒ **mọi mức tăng advisor đo trong thế giới này chứa một phần là giá trị của việc sửa một khuyết tật ĐƯỢC TIÊM VÀO** ⇒ **không đỡ được suy luận ngoại suy sang tài xế GSM thật** | **CAO** (cho claim ngoại suy) | **RF** (4 trích nguyên văn) |
| **D9** | `src/gsm_sim/behavior.py:37-40` (`ECON_WEIGHT = 0.7`), dùng ở `:97` | **Hệ số DUY NHẤT quyết định tiền tác động bao nhiêu lên hành vi** — tức đúng kênh mà **mọi kênh advisor** đi qua — là một số **đặt tay**, tự khai *"ASSUMPTION, hiệu chỉnh bằng gate test ≥30 seed"*, và là **HẰNG MODULE ngoài config** ⇒ `test_config_flags_wired` không thấy, dashboard không có slider, **mọi sweep không với tới** | TB | **RF** — **CHƯA SWEEP** |
| **D10** | `configs/pilot_dongda.yaml:163` (`wait_cap_min: 60`) ↔ `src/gsm_sim/behavior.py:256` (`queue_len > 3` **chép cứng**) | Config mô tả tham số như thứ **thay cho một HÀNH VI còn thiếu** (*"GIẢM ĐỂ MÔ PHỎNG ĐI TRẠM KHÁC"*), nhưng hành vi đổi trạm thật nằm ở một ngưỡng **không có trong config, không sweep được**. ĐO 8 seed: **3,6%** lượt đi trạm kết thúc bằng chờ đủ 60′ rồi rời đi với SOC nguyên ⇒ trần **đang BIND thật** | THẤP-TB | AG — tác giả tự xếp độ tin **THẤP** cho việc thuộc lớp |
| **D11** | `src/gsm_core/features/from_l1r.py:242` (`hours_per_day = 10.0`), `:278-279` | S5 tính `feasible` trên **quỹ giờ HƯ CẤU**: hằng 10h/ngày, **KHÔNG trừ giờ đã chạy hôm nay**, dù giờ online THẬT được đọc ngay `:272-275`. TÁI LẬP: tài xế chạy 10h × 3 ngày, hỏi T4 20:00, quota 5tr ⇒ budget **44,0h** ⇒ `feasible=True`; capacity trung thực (40h) ⇒ `feasible=False`. **Verdict ĐẢO CHIỀU trên 4 giờ ma**, và ví dụ đó mang **clawback 580.000đ** ⇒ đúng loại **claim THUẬN LỢI phải verify gắt nhất** | **CAO** (ngày S5 được nối) | AG (repro D) |
| **D12** | `src/gsm_core/solvers/penalty_explain.py:40,50-55,118` | **Khoản PENDING cộng vào "tổng đã trừ"**: 1 applied 50.000đ + 1 pending 200.000đ ⇒ digest *"2 khoản trừ, tổng 250.000đ"* — **sai 5×** so với tiền thực sự đã bị trừ. Tiền **CHƯA mất** được nói như **đã mất**, đúng chỗ quyền giải trình 48h quan trọng nhất | TB | AG (repro E) |
| **D13** | `penalty_explain.py:63-78,120-122` + `from_l1r.py:457-458` | **Dùng ngưỡng THƯỞNG làm ngưỡng PHẠT**: digest thật *"tỷ lệ nhận chuyến 84% đang dưới mức tối thiểu 85% theo chính sách"* — trong một advice **GIẢI THÍCH PHẠT** — tuy 85% là điều kiện **ĐỦ THƯỞNG** (`bonus_min_acceptance`), **không phải ngưỡng bị TRỪ TIỀN** ⇒ tài xế hiểu **sai nhân quả**. Cộng lệch chân trời (phạt gộp 30 ngày vs stat **1 dòng gần nhất**) | TB | AG (repro E) |
| **D14** | `mission_knapsack.py:45` (cost riêng từng mission) vs `world.py:781-789` (mission **PASSIVE**, một cuốc tiến độ **MỌI** mission active) · `:52-58` (bỏ `window_start/end`) | (a) Solver **định giá 10h cho việc world làm trong 5h** ⇒ bỏ lỡ mission 40.000đ; (b) **không có ràng buộc khung giờ** dù producer giữ đủ (`from_l1r.py:328-331`) ⇒ khuyên **theo đuổi một mission BẤT KHẢ THI TUYỆT ĐỐI** (còn 30′, cần 2,5h), báo `feasible=True` | **CAO** (ngày S6 được nối) | AG (repro B, C) |
| **D15** | `penalty_explain.py:72,78,111` (`'policy_v:threshold'` **literal**) vs `weekly_khoan.py:38` (`f'policy_v:{policy.version}\|basis={basis}'`) | **Không có policy version trong nguồn trích** — vi phạm trực tiếp §5 (*"nguồn có trích dẫn, có version"*). Đối chiếu được **trong CÙNG một lần chạy**: S8 `['…','policy_v:threshold',…]` vs S5 `[…,'policy_v:sim-policy-v0\|basis=gross']` | TB | AG (repro E) |
| **D16** | `src/gsm_core/solvers/f3_patterns.py:21-27` (`_dur_min` cộng trừ trên `iso[14:16]`, **không parse ngày**) | Hoạt động vắt nửa đêm cho **duration ÂM**: `_dur_min(23:40 → 00:20) = **−1400**`; ca sạc 17:40→00:20 (400 phút thật) ⇒ `duration_min = **−1040**`, severity MED thay vì HIGH, và **số ÂM đi thẳng vào `heuristic_note` gửi tài xế** + vào `numbers[]`. **Nặng hơn**: cổng `idle_peak dur >= 25` và `rest_likely dur < rest_min_dur` ⇒ **mọi pattern vắt nửa đêm bị LOẠI IM LẶNG** — mất hẳn, không phải sai severity. Archetype P7 **là ca tối-đêm** | TB (S3 mồ côi) | AG (repro) |
| **D17** | `src/gsm_core/solvers/anomaly_alert.py:42-45` (`except ValueError`) | Lệch aware/naive raise **`TypeError`** ⇒ **CRASH toàn bộ `solve()`** thay vì degrade. Tái lập được | TB | AG (repro — nâng conf từ TB lên CAO) |
| **D18** | `anomaly_alert.py:120` (`độ tin cậy {confidence:.0%}`) vs `mockgen/realdata.py:481` (`rng.uniform(0.3, 0.8)`, mọi bản ghi `status='open'`) | In cho tài xế một **phần trăm như xác suất đã hiệu chỉnh**, trong khi mock sinh nó **ngẫu nhiên** và **không có bản ghi nào chuyển cleared/closed** ⇒ **không có mẫu để tính precision**. Chạm thẳng §5 (*"mock phải gắn nhãn"*, *"không hứa chắc"*) | TB | AG |
| **D19** | `src/gsm_core/features/from_l1r.py:402-408` (map cầu **THƯA**, chỉ giờ CÓ cuốc) + `src/gsm_core/solvers/idle_reduction.py:76` (`demand.get(str(h), 1.0)`) vs `multiday.py:183` / `advice_bridge.py:820-822` (**đủ 24 giờ**) | Giờ **0 cuốc (vắng NHẤT)** bị chấm là **CAO ĐIỂM** và LOẠI khỏi khung nghỉ. TÁI LẬP cùng input: map L1R ⇒ `worst_window` giờ 11 (idle 30,0′); map sim ⇒ giờ 14 (idle 50,0′) — **hai lời khuyên khác nhau trên cùng một thế giới**. Default 1.0 là **CÓ CHỦ Ý an toàn** nhưng với map thưa thì *"không biết"* và *"bằng 0"* **bị gộp làm một** — đúng lớp hidden-fallback mà `gsm_core/policy.py:118-120` **tự cảnh báo** | **CAO** (cấu trúc) | AG (repro) |
| **D20** | `src/gsm_sim/world.py:998-1016` (chỉ pop `standby_plan` khi action ∈ {WAIT, RELOCATE}) vs `:1029-1041` (khối rest **không đọc** `standby_plan`) | S7 `rest_window` và S4 positioning **kéo cùng một actor hai hướng ở hai tick liền kề**; nhánh REST không chạm plan ⇒ plan **treo** ⇒ hai lần di chuyển tốn ENROUTE+SOC. **Cổng đặt SAI VỊ TRÍ**: khối standby quyết định *"có áp kế hoạch toàn cục không"* **TRƯỚC** bước duy nhất có thể biến REST thành RELOCATE | TB (kênh OFF) | AG — tần suất/chi phí **CHƯA ĐO** |
| **D21** | `ui/backend/app/services/demo_session.py:68-71` vs `configs/pilot_dongda.yaml:336` | **DEMO của Track UI BẬT `shift_plan: True` và `accept_lift: True`** dù ĐA-07 đã TẮT trong config pilot. Adherence ép 0 ⇒ **động lực học sim không đổi** (observer-only), **NHƯNG** nội dung S2 (lịch, `next_action`, digest *"E[payout]… hơn baseline…"*) **mang đủ S2-1/S2-2/S2-3 đi vào checkpoint artifact** — đúng bề mặt Cường xem. Cùng khuôn UPDATE-117 (*"cờ config nói SAI hành vi thực"*) | TB | AG — **CHƯA mở UI** xem có render ra card không |
| **D22** | `src/gsm_core/solvers/f3_patterns.py:95-105` (gọi `policy.next_tier_gap`, **không đọc** acceptance/completion dù đã có ở `:40-41`) | Hứa **115.000đ** cho tài xế mà luật trả **0đ** (`policy.py:94-97`). TÁI LẬP ở `acc=comp=0,84`. **Nặng hơn artifact mô tả**: ở `acc ∈ [0,50; 0,85)` **KHÔNG pattern nào khác bắn** ⇒ đúng vùng tài xế **đã rớt thưởng**, hệ thống vừa **IM về việc rớt** vừa **hứa 115k**. **GỐC SÂU**: `PolicyBundle` không có method nào áp eligibility — `bonus_at()` tự khai *"CHƯA xét ràng buộc tỷ lệ"*; 3/5 caller tự quấn cổng ngoài, **2 quên** | **CAO** (ngày S3 được nối) | AG (repro) |
| **D23** | `src/gsm_core/features/from_l1r.py:215` (ghi `fulfillment_rate`) vs `src/gsm_core/solvers/f3_patterns.py:41` (đọc `completion_rate`, default **1.0**) | Trên đường L1R **`completion` LUÔN = 1,0**. Đối chứng bất đối xứng: `from_l1r.py:124` (deriver `bonus_gap`) **DỊCH đúng**. ⚠ **HẠI KÉP — nó LÀM HỎNG CHÍNH CÁCH SỬA D22**: cổng eligibility mà `mm-10` đề nghị sẽ **PASS VÔ ĐIỀU KIỆN** ở vế completion ⇒ tài xế `acc 0,90 + fulfillment 0,40` (luật trả 0đ) **vẫn lọt qua cổng đã "sửa"**. Thêm: `completion` ở `:41` hiện là **BIẾN CHẾT** (1 hit, không dòng nào dùng) | **CAO** | **TÔI** (đọc cả 3 site) |
| **D24** | `src/gsm_sim/advice_bridge.py:1105-1175` (đọc trọn hàm `check_shift_extend`) | Khuyên **KÉO DÀI CA** để với một mốc thưởng mà luật trả **0đ** — `grep bonus_min_acceptance\|bonus_min_completion` trong file = **0 hit**. **Thực thể thứ hai của cùng primitive mù `next_tier_gap`**, chưa artifact nào gắn cờ (`grep eligib` trong `mm-02` = 0 hit) | TB (kênh OFF) | AG (đọc code + grep) — **CHƯA đo tần suất** |
| **D25** | `src/gsm_core/solvers/weekly_khoan.py:37,116,137` + `schemas/l3/weekly_khoan_input.schema.json:90-96` | `money_basis` được đọc nhưng **KHÔNG rẽ nhánh phép tính nào**: `solve(basis='gross')` và `solve(basis='driver_payout')` cho **solution GIỐNG HỆT**; chỉ chuỗi provenance đổi, và `caveat[0]` render **TỰ MÂU THUẪN**: *"Khoán tính trên driver_payout (doanh số)"*. **Vi phạm §5 trực tiếp**: đặt `basis=driver_payout` cho ra **ĐÚNG con số gross nhưng DÁN NHÃN payout** | TB (S5 mồ côi) → **CAO** ngày nối | AG (repro) |
| **D26** | `src/gsm_core/features/from_l1r.py:311` (fallback `trips_per_hour = 2.0`) | **1 CHỐNG 3**: ba site dùng 1,5 (có nhãn ĐO), riêng `from_l1r` dùng **2,0 không nguồn**. Với tài xế không lịch sử ⇒ `cost_h` thấp hơn **25%** ⇒ knapsack **nhồi thêm mission** | THẤP-TB | AG |
| **D27** | `src/gsm_core/solvers/shift_dp.py:176` (`pts0 = points_now // PBS`) | **Floor THỨ HAI**, ở **điểm KHỞI ĐIỂM** (khác `S2-1`/`S2-2` là floor transition): 58 → mô hình ở 55 (mất 3), 99 → 95 (mất 4). **Sát mốc 60 thì lệch 4 điểm là lệch ĐÚNG CHỖ QUYẾT ĐỊNH** | THẤP-TB | AG (probe B) |
| **D28** | `src/gsm_core/features/bonus_gap.py:38-39` (`acceptance = acc/(acc+dec) if … else 1.0`) | **Quy ước thứ TƯ** cho tỷ lệ nhận — **nằm trong chính module hợp nhất** `rates.py`, mà docstring `rates.py` ghi rõ *"n=0 ⇒ KHÔNG BAO GIỜ trả 1.0"*. ⇒ `derive_bonus_gap_input` sinh `acceptance_rate=1.0` khi chưa có app_event ⇒ `_bonus_eligible` **qua cổng thưởng** ⇒ hứa thưởng cho người chính sách sẽ không trả. **BẪY NẠP SẴN, chưa nổ** (0 call site sản phẩm). Song song `entities.py:122` (0/0→1.0) và `journey.py:235` (0/0→None) vẫn là hai quy ước nữa | TB | AG |
| **D29** | `src/gsm_core/features/bonus_gap.py:20` (`history` **không được đọc**, AST-proved) | Docstring `:26-27` **HỨA hai nhánh**; chỉ nhánh *"tự tính"* tồn tại. Caller đưa lịch sử **ĐÃ CHUẨN HOÁ** (vd ledger thật) **bị bỏ im lặng**. Đây là feature builder của **S1 — solver duy nhất chạy trên CẢ sim lẫn sản phẩm**. Rủi ro: một fix tương lai *"truyền history đúng vào"* **sẽ không có tác dụng và trông như đã fix** | TB | **RF** |
| **D30** | `src/gsm_core/lifecycle/projections.py:347` (`EXECUTION_KINDS = {"standby_followed"}`) | **0 consumer** (ref duy nhất = dòng khai); phép tách thật ở `sim_metrics.py:640` bằng **literal trần**. `UPDATE-113:49` khẳng định *"Hằng EXECUTION_KINDS khai tường minh"* ⇒ tài liệu **bán nó như cơ chế sống**. Thêm execution kind thứ hai ⇒ sửa hằng **không tác dụng gì**, adherence **đếm sai im lặng** — đúng họ lỗi UPDATE-113 vừa sửa | TB | **RF** |
| **D31** | `ui/backend/app/adapters/advisor.py:105` (`RATE_WINDOW_DAYS = 7`) vs `:84` (`[-7:]` literal) | Comment tại `:105` tuyên bố hằng này *"đồng bộ với `_hist_rate`, không tạo quy ước thứ hai"* — nhưng `_hist_rate` **chép literal**. Đổi hằng ⇒ cửa sổ TỶ LỆ và cửa sổ ĐIỂM/GIỜ lệch nhau **trong cùng một thẻ advice**, im lặng | THẤP (latent) | AG |
| **D32** | `src/gsm_core/advisor/llm_client.py:106` (`feature=''` **không dùng**) vs `composer.py:78-83` (**cố ý truyền**) | Chữ ký nói có **định tuyến theo feature** (model/nhiệt độ/prompt khác nhau cho F0..F3); client **bỏ qua hoàn toàn** ⇒ mọi feature dùng **cùng primary/fallback model** | THẤP (stack mồ côi) | AG |
| **D33** | `ui/backend/app/simulator.py:28` (`HANOI_BOUNDS`) | **1 ref = dòng khai**; comment **tự gọi mình là geo-fence**. Không có geo-fence nào; toạ độ ngoài Hà Nội không bị chặn, và người đọc module tin ngược lại | THẤP | AG |
| **D34** | `src/gsm_sim/dashboard_theme.py:21` (`SURFACE`) | **0 consumer**; giá trị sống ở `.streamlit/config.toml:6`. Module tự nhận là hệ token khoá toàn trang nhưng token nền là **bản SAO CHẾT**. Cùng cơ chế đã làm `trajectory.py` mang bảng màu thứ hai xung đột (UPDATE-117) | THẤP | **RF** |
| **D35** | `src/gsm_sim/advice_bridge.py:130` (`plan_next_bucket`) | Được **GHI** (`:634`), **không consumer nào đọc**. Ghi vào để không ai tưởng phần giải thích *"bước kế tiếp"* đã có đường ra UI | THẤP | AG |
| **D36** | `src/gsm_core/solvers/weekly_khoan.py` · `mission_knapsack.py` · `penalty_explain.py` — **0 import production** | Danh sách mồ côi ĐÃ BIẾT chỉ ghi **S3, S9**. Sự thật là **5/9**. `UPDATE-038/040` và `tracking/TODO.md` trình bày khoán tuần + mini-task như **năng lực đã có**; sản phẩm không có ⇒ ai lập kế hoạch demo dựa trên đó **hụt 3 use case**. ⚠ Thêm: `pipeline.py:59` nhận `solver_reports` **TỪ CALLER** (không có dispatcher tên→solve) ⇒ **thêm tên vào `router.py` KHÔNG làm solver chạy** | (là **CÂU HỎI CHO CƯỜNG**, không phải bug) | AG (đếm call site + đối chiếu tay) |

### 🔵 E. BỀ MẶT BẰNG CHỨNG / TÀI LIỆU — hỏng ở ĐƯỜNG DẪN BẰNG CHỨNG, nên mọi audit sau **TÁI NHIỄM** (5)

| ID | file:line | Hậu quả | Sev | Tự kiểm |
| --- | --- | --- | --- | --- |
| **E1** | `docs/reports/week2/Week2-Report-Driver-Advisor-Team.md:161`, `:166-174`, `:173` | **BÁO CÁO GỬI MENTOR** liệt kê đủ **9 solver KHÔNG có cột trạng thái** và nói *"Mọi con số tài chính hiển thị cho tài xế **phải** đi qua một trong chín solver này"*, trong khi **3/9 có 0 caller**. Ô `:173` khai S8 = *"Tra cứu + đối chiếu PolicyBundle có version"* — **SAI HAI LẦN** (`penalty_explain.py:38` chữ ký là `solve(pi)`, không nhận PolicyBundle; source `'policy_v:threshold'` **không version**). ⚠ **File CHƯA COMMIT — còn sửa được** | **CAO** (trung thực với bên ngoài) | AG (conf CAO cho `:173` là khai báo SỰ KIỆN; TB cho `:161` có thể đọc như luật chuẩn tắc) |
| **E2** | `configs/pilot_dongda.yaml:391-392` + `src/gsm_sim/advice_bridge.py:766-768` (chép y nguyên) | Giữ nguyên khẳng định *"hiệu ứng VÁCH ĐÁ… mất 34k"* mà **`UPDATE-048:11-32` ĐÃ RÚT ngày 2026-07-26** (đo lại 30 seed: nhóm KHÔNG chạm ngưỡng có Δpayout **+18.207đ, DƯƠNG**; *"−34k là một seed cá biệt"*), và `UPDATE-048:56-60` đo chính cổng dựng từ đó là **INERT**. **LAN TRUYỀN ĐO ĐƯỢC**: `mm-03 MI-8` đọc đúng hai dòng này rồi **dựng cả một finding lên nó**. ⚠ Chính file config này cách đó ~130 dòng **tự ghi bài học D-M3-15** (*"giữ một giá trị dẫn xuất trong config là mời gọi nó lệch lần nữa"*) | TB→**CAO** (vệ sinh bằng chứng) | AG (mở nguồn gốc ra đọc) |
| **E3** | `src/gsm_core/solvers/shift_dp.py:16` (`ACTIONS = ("ONLINE","REST","SWAP","END") # thứ tự = tie-break priority`) | Code chạy **ONLINE → SWAP → REST → END** (`:225,239,245,251`); đảo SWAP/REST là **CÓ CHỦ Ý** từ Cycle R/H3 nhưng `:16` không sửa theo. **Một dòng comment sai này chính là thứ làm người đọc — kể cả `S2-5` — suy ra dominance qua ONLINE thay vì qua REST ⇒ RA SAI ĐIỀU KIỆN REOPEN** (xem D1). Sửa **một dòng** | THẤP về code, **CAO về đòn bẩy** | AG |
| **E4** | `tracking/updates/UPDATE-097-c5-swap-co-gia.md:15` | Khẳng định *"consumer đủ số để tính net công bằng cho CẢ HAI lịch"* — **SAI dưới config ship** (xem D3: `n_swaps` không expose, tích có thừa số 0) | TB | **RF** |
| **E5** | `tracking/updates/UPDATE-078-…:44` + `research/audit/2026-08-06-math-model-audit/mm-07-s2.json:157` + `mm-07-s2-STAGED.md:112` | **BA artifact gắn nhãn `OBSERVED-CONFIG` confidence CAO** cho liên kết `orders.trip_km_median` — **một liên kết KHÔNG TỒN TẠI** — và một trong ba **dùng con số 3,5 đó để tính ra kết luận định lượng** *"chi phí ước NON ~40%"*. Đây là thiệt hại **vượt ra ngoài code** của cơ chế (1) | TB→**CAO** | **RF** + AG (probe3.py reproduce) |

---

## 3. ĐỀ XUẤT CYCLE — 10 cycle

**Nguyên tắc xếp đã áp dụng:** (i) chạm **ĐỘ TIN CỦA PHÉP ĐO** trước chạm giá trị · (ii) chạm **ĐƯỜNG SẢN
PHẨM** trước chạm sim · (iii) **CỔNG CHẶN TÁI DIỄN đi CÙNG cycle sửa thực thể đầu tiên của lớp** (không thì
lớp tái sinh) · (iv) cái đang **CHỜ CƯỜNG QUYẾT** không xếp vào cycle.

---

### CYCLE 1 — "MỘT KÝ ỨC, MỘT NHỊP" cho đường sản phẩm v2

- **Một câu:** Đường sản phẩm v2 đang chép lại luật nhịp/pha/ngân sách bằng ba bộ hằng riêng, nên một con số adherence của sản phẩm đang bị **cơ chế** làm lệch chứ không phải hành vi tài xế — cycle này xoá cả ba bản chép và dựng **test vi phân** để chúng không mọc lại.
- **Thuộc lớp:** cơ chế **(2) CHÉP LUẬT** (L1b). **Thực thể:** A2, A3, A4, A11.
- **Root cause đã chứng minh chưa:** ✅ **CÓ** cho cấu trúc — tôi tự mở cả bốn site (`advice.py:62-63,112-126`; `checkpoint.py:340-356`; `cadence.py:105`; `advice_checkpoint.py:569-584`; `checkpoint_store.py:302-309`). Phân kỳ pha reproduce bằng code thật (**4/4** mốc ca đêm, `rc-L1 m1`); phân kỳ nhịp reproduce bằng code thật (cửa sổ **+20..+29′**, `rc-L1 m6`). Hậu quả cross-day của A4 mới là **bằng chứng CẤU TRÚC**, chưa reproduce end-to-end.
- **Test đo-TRƯỚC (phải ĐỎ hôm nay):**
  1. Test vi phân `_phase_of` vs đường đọc của `get_advice` trên lưới **{ca ngày 06→22, ca chiều 14→23, ca đêm 22→02} × {early, mid, late}**.
  2. Test vi phân `evaluate_checkpoint` vs `cadence.evaluate` trên lưới **Δt = 0..60′** (61 điểm).
  3. Test dựng **7 checkpoint qua 2 NGÀY** cho một `driver_id`, đòi ngày 2 vẫn còn ngân sách; và một checkpoint `dismissed` ở pha `early` không được tắt topic ở pha `late`.
- **Acceptance BẰNG SỐ:** 3 test đỏ-trước → xanh-sau · cooldown v2 = **30′** khớp `cadence.effective_gap_min` trên **61/61** điểm lưới (hôm nay lệch ở 10/61) · `_phase_of` khớp đường đọc trên **9/9** ô lưới (hôm nay lệch ≥5/9) · `proactive_count` ngày N chỉ đếm checkpoint có `date == N` (**0 rò** trên fixture 3 ngày) · **BÁO Δ số thẻ hiển thị trước/sau** (đây là **đổi HÀNH VI sản phẩm**, không phải refactor).
- **Rủi ro đảo kết luận:** cooldown 20′→30′ và dismiss cả-ngày→hết-pha **sẽ GIẢM số thẻ**. ⇒ **mọi số adherence v2 đo TRƯỚC cycle này không so được với sau**. Phải chạy trước/sau và công bố Δ, không được lặng lẽ sửa.
- **Phụ thuộc:** không. **Đây là điểm vào.**

---

### CYCLE 2 — Đính chính 5 bề mặt bằng chứng đã bị rút / sai trạng thái *(docs-only, ~nửa ngày)*

- **Một câu:** Năm bề mặt "nguồn sự thật" đang biện minh cho code bằng những khẳng định đã bị chính repo rút hoặc chưa bao giờ đúng, và sự lan truyền đã **ĐO ĐƯỢC** (hai audit sau bị nhiễm).
- **Thuộc lớp:** không thuộc L1..L6 — hỏng ở **ĐƯỜNG DẪN BẰNG CHỨNG**. **Thực thể:** E1, E2, E3, E4, E5 (+ ghi lại điều kiện reopen mới cho D1, D2).
- **Root cause đã chứng minh chưa:** ✅ **CÓ** — lan truyền đo được **hai lần**: (a) `mm-03 MI-8` dựng nguyên một finding trên `yaml:391-392` (E2); (b) `S2-5` suy dominance qua ONLINE vì `shift_dp.py:16` (E3) ⇒ ra một **điều kiện reopen SAI**.
- **Test đo-trước:** không có test tự động — đây là docs. Thay bằng **danh sách kiểm 5 mục có `file:line` và nguồn đính chính**.
- **Acceptance BẰNG SỐ:** 5/5 bề mặt mang câu đính chính có trích `UPDATE`/artifact gốc · `Week2-Report` bảng 9 solver có **cột trạng thái** với **3 ô ghi "0 caller sản phẩm"** và ô S8 sửa 2 lỗi · `DEFERRED.md` ghi **điều kiện reopen MỚI** cho `D-E4-01` (`cash_cost` **INERT** trên [70;250], ngưỡng lật 4.325đ/km) và cho `S2-5`/`sp_end_only` (END bất khả **độc lập chi phí**, 0/225).
- **Rủi ro đảo kết luận:** thấp. Rủi ro **KHÔNG làm**: cao — mỗi vòng audit sau **tái nhiễm**, và một bề mặt **đang rời khỏi team**.
- **Phụ thuộc:** không. Chạy song song Cycle 1 (khác file).

---

### CYCLE 3 — CỔNG KHOÁ CONFIG, viết **MỘT LẦN** + 3 khoá ma

- **Một câu:** `Config.get(dotted, default)` trả default **im lặng** khi path vắng, và ba dòng code đang sống nhờ đó — cycle này nối đúng khoá và dựng **một** cổng tĩnh (không phải ba) để cơ chế không tái sinh lần thứ tư.
- **Thuộc lớp:** cơ chế **(1)**, bị **BA lớp** cùng nhận (L1(a) = L3(a) = L6b) ⇒ **cổng phải là MỘT FILE**. **Thực thể:** `advice_bridge.py:202`, `:209`, C9 (`test_m0_integrity.py:212`), + `world.py:172` (= `D-SIM-K6`, vào allowlist kèm link DEFERRED).
- **Root cause đã chứng minh chưa:** ✅ **CÓ, reproduce 1 lệnh** (`probe3.py`): đặt `demand.trip_km_median=9.0` ⇒ `avg_dist_km` **vẫn 3.5**; đặt `behavior.cancel_after_accept_rate=0.40` ⇒ `completion_prior` **vẫn 0.95**. Cơ chế định vị xuống `src/gsm_sim/config.py:32-40`.
- **Cổng đi CÙNG (nguyên tắc iii):** `tests/test_config_key_ton_tai.py` — **test_A** (hướng ngược code→config, khớp **ĐƯỜNG DẪN ĐẦY ĐỦ**) · **test_B** (sửa `test_moi_co_config_deu_co_nguoi_doc` từ khớp **TÊN LÁ** sang khớp đường dẫn đầy đủ) · **test_C** (assert 0 call site `cfg.get(<biến>)`) · **self-test bắt buộc** (fixture khoá ma phải làm scanner đỏ — tiền lệ `test_scanner_nay_THUC_SU_bat_duoc`).
  - ⚠ **test_B là lập luận mạnh nhất của cả năm artifact**: không có nó thì **chính dòng hỏng** `cfg.get("orders.trip_km_median")` lại làm cổng cũ tin rằng khoá đúng `demand.trip_km_median` **đã có người đọc** — *cổng bị chính lỗi nó đi bắt qua mặt*.
- **Acceptance BẰNG SỐ:** cổng ĐỎ với **đúng 4 dòng** hôm nay, **0 dương tính giả** (đã đo, `run9.py`/`scan2.py`, <1s) → xanh sau · sau khi nối đúng khoá, run mặc định phải **BIT-IDENTICAL** (default trùng giá trị config hôm nay) — **chứng minh bằng fingerprint, KHÔNG bằng lập luận** · một run với `demand.trip_km_median=5.0` phải làm `avg_dist_km`=5.0 (hôm nay Δ=0).
- **Rủi ro đảo kết luận:** ⚠ **CAO nếu bỏ qua** — sửa `avg_dist_km` **dịch mọi baseline E-series đã đo với `shift_plan` bật**. Phải **quyết định TRƯỚC**: đo lại arm E hay ghi nợ có nhãn. `L6-M1` đo lệch định giá **20,2%** (cận dưới, đo trên trung vị 3 seed).
- **Phụ thuộc:** không. **Là prereq của Cycle 10.**

---

### CYCLE 4 — CỔNG MỘT CHIỀU PHẢI **THỰC SỰ SOI** *(điều kiện tiên quyết của mọi phép đo kênh ngủ)*

- **Một câu:** Bảy khoá lan can/sổ cam kết mang nhãn *"cổng một chiều canh"* mà cổng **không hề soi** — bản vá trước chặn đúng chiều đi ra nhưng **không nối chiều đi vào**, và đây là lần **thứ ba** cùng một lỗi.
- **Thuộc lớp:** họ hàng cơ chế **(3)** — *cổng đọc như phủ mà không phủ*. **Thực thể:** C1.
- **Root cause đã chứng minh chưa:** ✅ **CÓ** — probe `c4b-do-vung-mu-tang-5.py` **tiêm "rail SỤP VỀ 0" cho TỪNG khoá** và đọc `keys` của `aggregate_health_guardrail` bằng `inspect`. ⚠ Và probe đó **cứu khỏi một số thổi**: đếm thô ra **11**, số đúng là **7** (vì `veto_calls_n`/`veto_fired_n` là **mẫu số và tổng** — cổng soi từng rail riêng nên **không soi là ĐÚNG THIẾT KẾ**).
- **Test đo-TRƯỚC:** test bất biến *"mọi khoá định tuyến một chiều (`_ONE_WAY_PREFIXES`) thì **HOẶC** được cổng soi **HOẶC** được khai trơ tường minh **có lý do**"* — phải ĐỎ với **đúng 7 khoá**.
- **Acceptance BẰNG SỐ:** vùng mù **7 → 0** · `xveto_calls_n` và `xveto_fired_n` **có mặt trong `a_mean`** (9 → 0 khoá vắng) · danh sách của `aggregate_health_guardrail` và vòng lặp `health_guardrail_flags` **SUY RA từ CÙNG nguồn** với `_ONE_WAY_PREFIXES` (0 dòng chép tay) · cổng có ca **sever-restore tự chứng minh đỏ được**.
- **Rủi ro đảo kết luận:** **thấp hôm nay** — `shift_extend`/`rest_window` đang TẮT nên 9 khoá đều 0 ⇒ **0 thay đổi số**. Rủi ro **KHÔNG làm**: mọi cycle đo lại kênh ngủ **chạy mù**.
- **Phụ thuộc:** không. **BLOCKS Cycle 10** và mọi thí nghiệm bật kênh ngủ.

---

### CYCLE 5 — "CARD KHÔNG ĐƯỢC GIẤU LÝ DO TỐN TIỀN NHẤT" *(đường sản phẩm S1)*

- **Một câu:** Tầng sản phẩm dựng lại lý do infeasible bằng **first-match** và nuốt `sensitivity` ở nhánh `already_maxed`, nên card nói với tài xế **một lý do** trong khi solver đã ghép đủ, và đúng vế cứu được tiền là vế bị giấu.
- **Thuộc lớp:** **PROXY SAI ĐẠI LƯỢNG** (lớp thứ 7 — 3 artifact độc lập cùng đề nghị; xem §1c). **Thực thể:** A1, A7, A5.
- **Root cause đã chứng minh chưa:** A1 ✅ **CÓ** (tôi tự đọc `:321-327` và `:389-399`; `mm-03` tái lập bằng `advisor.advice('d-1','2026-07-05',1200)` **thật**). A7 ✅ tái lập bằng input tổng hợp, nhưng **phơi nhiễm hôm nay = 0**. A5: số **19,5%** là đo của `mm-03`, **tôi CHƯA đo lại**.
- **Test đo-TRƯỚC (phải ĐỎ hôm nay):**
  1. Fixture `enough_hours=False ∧ ok_acceptance=False` ⇒ đòi `reason_code` và message mang **ĐỦ** mọi ràng buộc False.
  2. `solve(points=200, acc=0.86, comp=0.95)` (`already_maxed ∧ acceptance ∈ [0,85; 0,88)`) ⇒ đòi `sensitivity ≠ []` và `_cliff_item(...) is not None`.
- **Acceptance BẰNG SỐ:** quét lại **đúng cửa `advisor.py:227`** (chỉ prefix `d-`/`r-`), t=20:00: số ca card chỉ nói 1 lý do **549 → 0**; ⚠ **PHẢI ĐO LẠI trong cycle, KHÔNG trích thẳng** — lượt quét đầu của `mm-03` đếm cả `ce-*` và ra **1.079 ca / 36,3 triệu đ**, **sai ~2×**; số đúng sau khi lọc theo cửa thật là **549 ca / 17,58 triệu đ**.
- **Rủi ro đảo kết luận:** ⚠ **A5 KHÔNG được "sửa" bằng cách cho UI dùng tỷ lệ trong-ngày** — leak-free as-of là **ĐÚNG** (ĐA-01) và một phần lật **19,5% là TẤT YẾU**, không phải bug. Cái sai là **CÂU CHỮ intraday** (*"vài lần từ chối nữa…"*) trên một con số **không nhúc nhích trong ngày**. Sửa sai chỗ sẽ **phá ĐA-01**.
- **Phụ thuộc:** không. Nên đi ngay sau Cycle 1 (cùng đường sản phẩm).

---

### CYCLE 6 — MỘT PRIMITIVE **BIẾT ĐIỀU KIỆN THƯỞNG**

- **Một câu:** `PolicyBundle.next_tier_gap`/`bonus_at` **mù eligibility** và 2/5 caller quên quấn cổng, nên hệ thống hứa thưởng mà luật trả 0đ — nhưng **phải sửa key `fulfillment_rate`/`completion_rate` TRƯỚC**, nếu không cổng mới **thủng một nửa**.
- **Thuộc lớp:** cơ chế **(2)** (hai quy ước cho một khái niệm, chỗ nối có **MẶC ĐỊNH THUẬN LỢI nuốt sai lệch**). **Thực thể:** D23 (**phải sửa trước**), D22, D24.
- **Root cause đã chứng minh chưa:** D23 ✅ **CÓ** — tôi tự đọc cả ba site (`from_l1r.py:215` ghi `fulfillment_rate`, `f3_patterns.py:41` đọc `completion_rate` default **1.0**, `from_l1r.py:124` dịch đúng ⇒ **bất đối xứng ở ĐÚNG deriver của S3**). D22 ✅ tái lập ở `acc=comp=0,84`. D24 ✅ ở mức code (đọc trọn hàm + grep = 0 hit), **CHƯA đo tần suất**.
- **Test đo-TRƯỚC (phải ĐỎ hôm nay):**
  1. `dse` đường L1R có `fulfillment_rate=0,40` ⇒ đòi solver thấy `completion = 0,40` (hôm nay đọc 1,0).
  2. `acc=comp=0,84` ⇒ đòi `bonus_progress_gap` **KHÔNG bắn** (hoặc bắn kèm caveat "thưởng đang không đủ điều kiện").
  3. Tài xế `acc=0,90 ∧ fulfillment=0,40` ⇒ đòi **không** có lời hứa thưởng (đây là ca chứng minh cổng **không** thủng nửa).
  4. `check_shift_extend` với `acceptance < bonus_min_acceptance` ⇒ đòi trả một `reason` mới, không đòi kéo ca.
- **Acceptance BẰNG SỐ:** 4/4 test đỏ→xanh · `grep 'completion_rate'` trên đường L1R = **0 mismatch** · số caller của `next_tier_gap` **không** quấn cổng: **2 → 0** (hoặc primitive được đổi tên thành `attainable_tier_gap(points, acceptance, completion)` và `next_tier_gap` khai tử) · `completion` ở `f3_patterns.py:41` **không còn là biến chết**.
- **Rủi ro đảo kết luận:** ⚠ **Sửa D22 mà không sửa D23 trước = dựng một cổng thủng một nửa rồi tưởng đã đóng.** Đây là phản biện của `mm-10` với chính đề xuất sửa của `mm-10`.
- **Phụ thuộc:** không. Bán kính hôm nay = 0 (S3 mồ côi, `shift_extend` OFF) ⇒ xếp sau Cycle 5.

---

### CYCLE 7 — "MỘT TÊN MỘT CÔNG THỨC" cho `net_mean_all` + sổ `MONEY_PARAMS`

- **Một câu:** Hai công thức khác nhau đang mang cùng tên `net_mean_all` và trùng nhau **chỉ vì `cost_vnd ≡ 0`**, nên ngày ai bật giá thì artifact E10/L104 sẽ **im lặng báo GROSS dưới tên NET** trong khi `parallel.py` báo NET thật.
- **Thuộc lớp:** cơ chế **(2)** trên tầng ĐO (refuter **chuyển `L5-04` từ L5 về L1**: *"zero-cost không TẠO ra lỗi — nó CHE một lỗi đã có sẵn"*). **Thực thể:** C2, C3, D3, D4.
- **Root cause đã chứng minh chưa:** ✅ **CÓ** — `probe_l5.py`, 5 seed: bật `cash=150 + fee=9.000` ⇒ phân kỳ **23.685 / 24.189 / 24.303 / 24.597 / 25.603đ** (9,8–10,3% payout).
- **Cổng đi CÙNG (nguyên tắc iii):**
  - **G1** — stub `RunResult` với `cost_vnd > 0`, gọi **MỌI producer** của khoá `net_mean_all`, assert bằng nhau; + phần **COMPLETENESS** tự grep `net_mean` để producer thứ 4 **thấy đỏ ngay**.
  - **G2** — sổ `MONEY_PARAMS = [(tên, dải_giá_THỰC, nguồn_trích_dẫn)]`, quét độ nhạy trên **cận dưới/cận trên của dải THỰC**, dán nhãn **LIVE/INERT** vào **snapshot JSON commit trong repo**. ⚠ **Assert NHÃN KHỚP SNAPSHOT, KHÔNG assert "phải LIVE"** — đây là điểm làm nó tuân **kỷ luật 4** và không tạo áp lực vặn tham số. + COMPLETENESS: quét khoá khớp `/_vnd($|_per_)/` trong `shift_dp.DEFAULT_PARAMS` + config; khoá chưa khai ⇒ FAIL.
  - **G3** — `solve()` **LUÔN** trả `terms_active`; cấm trạng thái *"ACTIVE với giá trị 0"* im lặng; thêm `n_swaps` vào `solution`.
- **Acceptance BẰNG SỐ:** G1 đỏ trước (lệch **~24.000đ**) → xanh sau · snapshot lần đầu ghi `cash_cost_vnd_per_km = INERT` trên [70;250] (**bất biến từng bit**) và `swap_fee_vnd = LIVE` (**102 → 12** bucket SWAP) — hai nhãn này là **DỮ LIỆU, không phải mục tiêu** · rà **13 file** dùng `net_mean` không caveat.
- **Rủi ro chưa ai nêu (refuter tìm ra):** ⚠ **G3 đổi SHAPE output của solver** ⇒ phải kiểm `schemas/` và `test_solver_report_schema` **TRƯỚC**. **Đây là đổi CONTRACT, không phải thêm test.**
- **Phụ thuộc:** không. **Phải đứng TRƯỚC mọi cycle bật giá.**

---

### CYCLE 8 — CỔNG ĐỘT BIẾN SENTINEL *(làm cho suite phân biệt được đúng với sai)*

- **Một câu:** 1.356 test xanh nhưng thay **mọi trường số** của `sensitivity` bằng −999999.0 vẫn xanh — cycle này dựng cổng đột biến đã-chạy-thật để mỗi con số hiển thị cho tài xế có **ít nhất một test ghim giá trị**.
- **Thuộc lớp:** cơ chế **(3)**. **Thực thể:** C5, C6, C7, C8, C10, C11, C13, B5.
- **Root cause đã chứng minh chưa:** ✅ **CÓ, và có ĐỐI CHỨNG DƯƠNG TÍNH** — `mutant_ctrl` đặt `delta_payout := -999999.0` ⇒ **1 failed** (chứng minh bộ đo **tới được** mã sản xuất), nên các kết quả *"vẫn xanh"* không phải do plugin không chạy.
- **Nội dung:** sổ đăng ký khai bộ ba (đường-dẫn-trường-output, sentinel, danh sách test PHẢI đỏ); chạy từng ca trong **subprocess**; + **luật completeness** (mọi khoá `solution` và mọi trường số `sensitivity` của 9 solver phải có trong sổ **HOẶC** `MIEN_TRU` kèm lý do) + **luật lệch đơn vị** (bảng unit rút **tự động** từ các lời gọi `_num(...)`) + **C11 rẻ nhất**: đổi `_cases()` sang **quét `derive_*_input_l1r`** ⇒ mọi deriver tương lai **tự vào cổng**.
- **Acceptance BẰNG SỐ:** sổ **≥20 ca**; **≥5 ca ĐỎ ngay lần chạy đầu** (C5, C6, C7, C8, B5 đã biết là đỏ) · cổng có ca **sever-restore tự chứng minh đỏ được** (tiền lệ `test_future_leak_gate.py::test_cong_nay_THUC_SU_bat_duoc_ro_ri`) · thời gian chạy theo NHÓM **≤6 phút** (đo được: 0,3s shift_dp – 1,6s ui range – 7,5s fairness – 202s 11 file solver) ⇒ **gate trước merge/nightly, KHÔNG đặt trong suite nhanh mặc định** · `_cases()` sau khi sửa phủ **8/8** deriver (hôm nay 7/8).
- **Rủi ro đảo kết luận:** thấp. ⚠ **KHÔNG kèm** luật AST *"parametrize trang trí"* — xem §4.4.
- **Phụ thuộc:** không. Nên đứng **trước** Cycle 9 và 10 (cả hai đều dựa vào suite để tin kết quả).

---

### CYCLE 9 — LIỀU và TRẦN của kênh **ĐANG SHIP** (positioning / S4)

- **Một câu:** Kênh duy nhất đang bật mặc định bị chi phối bởi một trần tính bằng công thức **khác hẳn** kernel phục vụ của dispatcher, và **43–51%** slot khan bị đốt cho người không đi — nên `+6.016đ` cần được **phân rã** trước khi nó được dùng làm căn cứ ship.
- **Thuộc lớp:** cơ chế **(2)** [B1] + **L4** [B2] + *"chỉ số không thể báo xấu"* [B5, B6]. **Thực thể:** B1, B2, B3, B4, B5, B6, B7, B8, B9, B10.
- **Root cause đã chứng minh chưa:** B1/B2/B9 ✅ **CÓ về CẤU TRÚC** (tôi tự đọc `market_state.py:78-90`, `world.py:472-503`, `capacity_alloc.py:77`). ⚠ Các **histogram** (87,6–88,2% `slots=0`; 43–51% đốt; 32,2–44,4% mover) đo trên **3 seed** — **đủ cho quyết định ĐO TIẾP, KHÔNG đủ cho quyết định SHIP**.
- **Test đo-TRƯỚC:**
  1. **RẺ NHẤT, chưa ai làm:** paired Δpayout **tách mover / non-mover** trên chính artifact seed 3000–3099 (n=100 đã có).
  2. Test đòi `capacity_alloc.solve` **đọc `params`** hoặc **gỡ tham số** khỏi chữ ký.
  3. Test sensitivity capacity **ở điểm vận hành thật (`capacity=1`)** đòi `n_unassigned ≠ baseline`.
- **Acceptance BẰNG SỐ:** (1) công bố `+6.016đ` **tách thành Δ_mover và Δ_non-mover với CI95 riêng** — nếu Δ_non-mover ≈ 0 thì cách đọc "tập trung ~35%" đúng; nếu SIG thì cách đọc "lan toả hệ thống" đúng; **hai cách đọc dẫn tới hai quyết định ship KHÁC HẲN NHAU** · (3) sensitivity ở `capacity=1` đổi **≥1/48** lô (hôm nay **0/48**) · (2) `params` không còn là cửa giả.
- **Rủi ro đảo kết luận:** ⚠ **CAO.** `E-S4-4` (pool theo ring — sửa B1) **SẼ đổi liều mạnh** ⇒ **regate ĐA-08 n=100 bắt buộc**, và kênh dương hiện tại **đang chạy ở điểm vận hành ĐÓI TRẦN** ⇒ **tăng liều có thể ĐẢO DẤU**. Ai sửa B1 phải sửa `market_state.py:86` (`sd = λ/(eff+1)`) **cùng lượt**, nếu không thừa hưởng biên sai (B10).
- **KHÔNG làm trong cycle này:** `E-S4-3` (TTL) và `E-S4-2` (ngân sách outflow) — xem §4.7, §4.8.
- **Phụ thuộc:** **sau Cycle 4** (nếu muốn tin guardrail), **sau Cycle 7** (nếu muốn tin `net_mean_all`), **sau Cycle 8** (nếu muốn tin suite).

---

### CYCLE 10 — ĐO LẠI `shift_plan` bằng solver **đã sửa** *(trả nợ một phán quyết dựa trên bằng chứng đã hỏng)*

- **Một câu:** Bản án ĐA-07 giết `shift_plan` được tuyên bởi một DP **mù mốc thưởng**, và chưa ai đo lại bằng solver đã sửa.
- **Thuộc lớp:** không phải lớp — là một **PHÁN QUYẾT** dựa trên bằng chứng hỏng.
- **Bằng chứng (git, không suy luận):** ĐA-07 tắt `shift_plan` `5a44cbb` **2026-07-28**; sửa `points_band_size` 15→5 (`ADV-01`) `bec2671` **2026-08-06**. Ở band 15, `shift_dp.py:207` là `pb + add_pts // PBS`; giờ thường `pph=5`, `exp_trips ≈ 1–2,2` ⇒ `add_pts ≈ 5–11` ⇒ `add_pts // 15 = **0**` ⇒ **points_band ĐÓNG BĂNG suốt DP** ⇒ mốc thưởng ngày **không bao giờ vào được giá trị Bellman**. Chính comment repo nói vậy (`shift_dp.py:21-25`: *"lịch S2 tối ưu như thể không có thưởng"*).
- ⚠ **Tôi KHÔNG kết luận "S2 thật ra có giá trị."** Rất có thể nó vẫn vô dụng. Khẳng định **hẹp hơn và chắc hơn**: **CHƯA AI đo `shift_plan` bằng solver đã sửa**, và bản án hiện hành dựa trên bằng chứng đã hỏng **ở đúng cơ chế mà kênh này tồn tại để khai thác**.
- **Điều kiện cần (do chính `mm-07` ghi + vòng này bổ sung):** `S2-2` (dư `add_pts % 5` vứt **mỗi bucket**, kỳ vọng mất **2 điểm/bucket** — `DERIVED, chưa đo`) · `S2-3` (cap `service_min` bind **96,0%**, eo phẳng 2,4 — **và SỐNG cả trên shape sản phẩm 88,6%**, A12) · **Cycle 3** (`avg_dist_km` nối đúng) · **Cycle 4** (cổng một chiều) · **D1** (END bất khả ⇒ nếu không sửa thì `sp_end_only` vẫn đo một kênh **không bao giờ phát lời khuyên**) · **D2** (`cash_cost` INERT ⇒ **đừng sweep** {0,70,150,250}).
- **Acceptance BẰNG SỐ:** chạy **CẢ HAI arm TƯƠI** (**không** so thẳng với số cũ — giữa 28/07 và 06/08 có hàng chục fix khác ⇒ đây là **phép đo MỚI**), **n=100**, báo kèm `decision_adherence` per archetype và **tầng 5 đầy đủ** (sau Cycle 4). **Prereg tiêu chí PASS/NO-GO trước khi chạy.**
- **Rủi ro đảo kết luận:** ⚠ **CAO nhất trong 10 cycle** — đây là cycle duy nhất có thể **LẬT một phán quyết đã đóng** (cũng có thể **củng cố** nó). ⚠ ĐA-07 bác ở n=100 **không chỉ bằng Δ** mà bằng `served −0,33đp SIG`, đơn chết `+4,1 SIG`, Gini/HHI xấu SIG — nên tiêu chí đo lại **phải phủ cả bốn**, không chỉ payout.
- **Phụ thuộc:** **3, 4, 7, 8** + fix D1, D2.

---

## 4. Cái gì **KHÔNG** làm và vì sao *(để không ai đào lại)*

1. **KHÔNG lập 6 cycle theo 6 lớp.** ĐO: 54 suất thực thể khai → **~36 phân biệt** (thổi ~50%); 3 dòng code bị đếm **2–3 lần**; **L1(a) = L3(a) = L6b là MỘT cơ chế** với **BA đề xuất cổng trùng nhau**. Lập **cổng khoá-config MỘT FILE**, không phải ba file test.
2. **KHÔNG xếp ưu tiên theo SỐ THỰC THỂ mỗi lớp** — cùng lý do. Xếp theo **bán kính ảnh hưởng thật**.
3. **KHÔNG dựng cổng CANON literal** (`rc-L1` cổng B). **ĐO: 165 lần bắn / 50 file**, tỷ lệ phải MIỄN TRỪ **~97,6%** so với **ngưỡng huỷ 10% do chính L1 tự đặt** ⇒ hỏng **gấp ~10 lần** ngưỡng. Và **không thu hẹp được**: ba giá trị ồn nhất (6, 20, 30) **chính là ba giá trị của khuyết tật đầu bảng** ⇒ mọi cách thu hẹp đánh đổi giữa **ồn** và **mù**.
4. **KHÔNG dựng luật AST "parametrize trang trí"** (`rc-L2` phần B luật 1). **ĐO: bắn 6/38 test — chấp nhận được — nhưng BẮN TRẬT MỤC TIÊU**: `test_cong_thuc_dung_tren_nhieu_muc_soc` **KHÔNG** nằm trong danh sách, vì assert dòng 172 **CÓ chứa `soc`**. Cái sai của nó là *"vị ngữ đúng với **mọi** soc>0"* — **một tính chất AST thuần không quyết định được**. Dùng cổng đột biến (Cycle 8) thay.
5. **KHÔNG mở cycle "sửa lớp L6".** 4/7 thực thể trùng hoặc **tự nhận thuộc lớp khác**; hai thực thể **mạnh nhất tự nhận không phải L6**; 3 cái còn lại (M3 liều · M4 giữ-khuyết-tật · M5 hằng-không-neo) **không chung cơ chế runtime** — *"bù cho khuyết tật"*, *"giữ khuyết tật"* và *"hằng không có neo đo"* là **ba việc khác nhau ở runtime**. ⇒ M3/M4/M5 vào `DEFERRED` như **3 nợ đơn lẻ** (D7, D8, D9), **M4 đáng chú ý nhất** vì nó chạm khả năng ngoại suy và **có một TEST đang CẤM thu hẹp khuyết tật**.
6. **KHÔNG dựng "sổ hiệu chỉnh có hạn dùng"** (`rc-L6` phần b). Phản-tác-dụng **do chính L6 nêu**: khi test đỏ, cách sửa rẻ là **VẶN CONFIG cho khớp phép đo — đúng chính lớp L6**. Cộng thêm: biến **5 lần `run_once`** thành phụ thuộc của suite, và **sẽ đỏ khi thế giới đổi HỢP LỆ** ⇒ đúng mẫu cổng sẽ bị người sau **nới dung sai hoặc `mark skip`**.
7. **KHÔNG làm `E-S4-3` (TTL cho assignment).** **ĐÃ BỊ BÁC 2026-08-07** (`D-ADV-01` đính chính, `DEFERRED.md:126`): **0/179** lượt vượt biên bucket, và `market_state.py:162-167` **cache theo bucket** ⇒ re-validate đọc lại **Y NGUYÊN ảnh cũ** ⇒ bản vá **BẤT ĐỘNG ở 100% lượt**. `mm-08` (2026-08-06) **đi TRƯỚC** đính chính nên tái phát nguyên văn cả claim lẫn bản vá. ⚠ **Ai lập plan từ `mm-08` sẽ đưa một cycle NO-OP vào hàng đợi.** Đo độc lập vòng này: lag p50 **1,3–1,7 phút**, max 54,4 phút, **0/113** lượt > 60 phút.
8. **KHÔNG làm `E-S4-2` (ngân sách outflow `max(0, eff−slots)`) như đã viết** — **gần-NO-OP**: ca `eff==slots` chỉ **7,7–12,9%** lượt gán; ca **thống trị** là `slots==0` (**34–42%**), ở đó `eff−slots ≥ eff` ⇒ **ngân sách không ràng buộc gì**. `mm-08-B` **quy sai nguyên nhân**; nguồn thật là B1.
9. **KHÔNG sweep `cash_cost_vnd_per_km` trên {0, 70, 150, 250}.** **ĐO: bất biến TỪNG BIT** trên [0; 4.325]đ/km, 36 case ⇒ **cả 4 mức cho kết quả Y HỆT** ⇒ sweep trả **Δ = 0** và **rất dễ đọc thành "chi phí không quan trọng"**. Cả `D-E4-01` và `PLAN-cycle-wx-2026-07-29.md:108` **đang đề xuất đúng phép sweep này** ⇒ phải đính chính (Cycle 2).
10. **KHÔNG dùng "bật chi phí lên" làm cách sửa L5.** Mệnh đề trung tâm của charter (*"bật giá thật thì phanh sống lại"*) **bị chính L5 bác bằng đo** với `cash_cost`. **Chỉ `swap_fee` là phanh thật.** Cái hỏng không phải **CON SỐ 0** mà là **CẤU TRÚC "chỉ một nhánh có tiền"**. Sửa bằng **bắt KHAI BÁO** và **bắt ĐỐI CHIẾU hai thế giới** thì trúng đích.
11. **KHÔNG coi `E-05`/`sp_end_only` là được sửa theo L5.** ĐO: **END = 0/36** ở **CẢ 9 tổ hợp** `cash{0,250,4400} × fee{0,9000,50000}`, và **0/225** ở 7 mức chi phí kể cả 1.000.000đ/km. Nguyên nhân là **THỨ TỰ** (REST xét trước END, so sánh chặt), **không phải chi phí**. **`README.md:17` của charter xếp SAI.**
12. **KHÔNG mở cycle cho 9 ký hiệu mồ côi của L3(b)** (D30..D35 + đồng loại). L3 **tự nhận** *"không có cơ chế runtime nào cả — nó là sự vắng mặt"* và **không đề xuất cổng**. ⇒ **backlog dọn dẹp**, không phải cycle. **KHÔNG** dựng test tổng quát *"mọi ký hiệu khai báo phải có consumer"*: nhãn `MEASURED`/`EXTERNAL` trong `schemas/l2/*` là **dành cho dữ liệu thật CHƯA có, không phải rác** ⇒ allowlist sẽ phình tới mức **trở thành chính thứ nó canh**.
13. **KHÔNG tự quyết S5/S6/S8 + `AdvisorPipeline` (D36).** Đây là **CÂU HỎI CHO CƯỜNG**: khai tử hay nối stack advisor. Theo **nguyên tắc (iv)**, không xếp vào cycle. ⚠ Kèm dữ kiện để quyết: `pipeline.py:59` nhận `solver_reports` **TỪ CALLER** ⇒ **thêm tên vào `router.py` KHÔNG làm solver chạy**; và với S5 thì **sim không có cơ chế khoán tuần/clawback** (`grep 'weekly_quota|khoan|clawback' src/gsm_sim/` = **0 kết quả**) ⇒ **ĐA-08 KHÔNG chạy được** cho S5/S8 bằng twin-world hiện tại ⇒ fix không phải *"nối dây"* mà là **tuyên bố KHÔNG THỂ KIỂM** theo `specs/real-data/data-contract-counterfactual.md §4`. Riêng **S6 thì world ĐÃ có mission** ⇒ đo được bằng ĐA-08 **ngay**, nhưng **phải sửa cost model (D14) trước**, nếu không lặp lại đúng `station_choice`.
14. **KHÔNG "sửa" A5 bằng cách cho UI dùng tỷ lệ trong-ngày** — xem Cycle 5, rủi ro.
15. **KHÔNG dùng `ranked_cells` / `herding_avoided` làm bằng chứng kênh hiệu quả.** Thứ tự `sd_ratio` bị **vứt HAI LẦN**; **76,1–79,8%** của `herding_avoided` là `unassigned` = người **không được khuyên gì** (đứng nguyên = y hệt arm đối chứng) ⇒ trích nó là **vòng tròn**.
16. **KHÔNG chiết khấu `+6.016đ` tuyến tính theo độ phủ pilot** (B4) — thứ chặn là **TRẦN** chứ không phải người. ⚠ Nhưng cũng **KHÔNG được suy từ liều ra tiền** (kỷ luật 4) — B4 mới đo **liều**.
17. **KHÔNG đề xuất overbooking `1/p̄` cho NGƯỜI THẬT.** Chỉ hợp lệ **TRONG SIM** để đo trần giá trị; phát lời khuyên **phân biệt theo adherence cá nhân** cho người thật là vấn đề **fairness**, phải Cường duyệt riêng.
18. **KHÔNG đưa sức khoẻ vào objective** (§1.2b). Mọi lan can nghỉ/mệt giữ nguyên là **cộng một chiều tier-5**. ⚠ Câu hỏi *"thời gian chờ/đổi pin có tính là nghỉ phục hồi?"* (từ `mm-07`) là **CHÍNH SÁCH, không tự quyết** — nới cổng nghỉ là **làm YẾU lan can một chiều** ⇒ phải qua `policy_locks` + duyệt.
19. **KHÔNG chốt "lớp thứ 7"** dù 3 artifact độc lập cùng đề nghị (§1c) — chưa ai truy nó grep-driven như L1..L6. Ghi lại như **giả thuyết**, không đưa vào kế hoạch.

---

## 5. Cảnh báo trung thực — cái gì trong bản đồ này **vẫn là suy luận chưa đo**

1. **Tôi tự mở code kiểm 12 vùng** trong vòng này: `advice.py:58-66,110-132` · `checkpoint.py:340-356` · `cadence.py:98-112` · `advice_checkpoint.py:52-64,203-228,342-356,565-590` · `checkpoint_store.py:298-314` · `main.py:38-50` · `advisor.py:315-335,386-402` · `from_l1r.py:120-128,210-220` · `f3_patterns.py:38-44` · `world.py:472-503` · `market_state.py:78-90`. **Mọi CON SỐ SIM trong bản đồ này là TRÍCH LẠI của agent, KHÔNG phải tôi đo.**
2. **Con số "~36 thực thể phân biệt" của refuter là ƯỚC LƯỢNG**, không phải phép đếm cơ học — refuter tự ghi *"ai dùng phải đếm lại"*. **Con số 76 của tôi là phép đếm CỦA TÔI trên bảng tôi dựng**, và nó phụ thuộc chỗ tôi kẻ ranh giới *"một thực thể"*. Ai trích phải đếm lại theo tiêu chí của mình.
3. ⚠ **`rc-L4.json` KHÔNG TỒN TẠI.** 5/6 lớp có artifact; **L4 không có, và refuter cũng không ra verdict cho L4**. Mọi thực thể L4 trong bản đồ này (B2, D14, D20, A7) đến từ người **đọc-nốt `mm-*`**, **KHÔNG** từ một vòng truy lớp có hệ thống. Nếu L4 là lớp thật thì bản đồ này **thiếu một mảng**, và mảng đó nằm **ĐÚNG trên kênh đang ship** (B2).
4. **Lớp thứ 7** (§1c) phủ **4/6, 4/9, 3/8** phần *"KHÔNG thuộc lớp nào"* của ba artifact. Nếu đúng, bảng 6 lớp **đang bỏ sót khuôn LỚN NHẤT rồi gọi phần còn lại là "nợ độc lập"**. **Tôi KHÔNG chốt.**
5. **`1,205` (rc-L1) và `1,5005` (rc-L6) là hai phép đo trên HAI MẪU SỐ KHÁC NHAU**, và refuter chỉ ra **CẢ HAI đều chưa phải mẫu số CHÍNH XÁC** của `count_supply` (`IDLE+enroute` **loại cả thời gian `ON_TRIP`**). ⇒ **`1,5005` phải đọc là "không mâu thuẫn", KHÔNG phải "đã xác nhận"**. Vế còn **sống ở CẢ HAI phán xử**: `bonus_feasibility._hour_rate` ước cao **~18%** trên mẫu số ĐỒNG HỒ (A6). Muốn chốt thì phải đo cuốc/giờ **trên đúng sổ IDLE+enroute**.
6. **Nhiều số của `mm-08` đo trên 3 seed** (87,6–88,2% `slots=0`; 43–51% đốt slot; 32,2–44,4% mover). **Đủ cho một quyết định ĐO TIẾP, KHÔNG đủ cho một quyết định SHIP.**
7. **Con số 549 ca / 17,58 triệu đ (A1) đã sập một bẫy trong chính vòng đó**: lượt quét đầu đếm cả `ce-*` (car/premium) mà `advisor.py:227` **chặn ngay từ cửa** ⇒ ra **1.079 ca / 36,3 triệu**, **sai ~2×**. ⇒ **PHẢI ĐO LẠI trong Cycle 5**, không trích thẳng.
8. **`file:line` PHÂN RÃ RẤT NHANH.** `mm-09` phát hiện `from_l1r.py` lệch **hệ thống ~+20 dòng** do commit `96a89d3` **cùng ngày audit**; `mm-03` phát hiện `_cliff_item` stale (artifact ghi `326-345`, thật là `367-386`). ⇒ **mọi `file:line` trong bản đồ này phải neo lại bằng NỘI DUNG (tên hàm / chuỗi literal) khi thi công**, không neo bằng số dòng.
9. **CHƯA AI quét `ui/web` (JS) và `ui/driver_app` (Dart) bằng AST** — cả 5 artifact lớp + refuter + 5 người đọc-nốt **đều tự nhận**. Nếu có thực thể ở đó thì bức tranh **đường sản phẩm còn thiếu một mảng**, và nó sẽ **LÀM MẠNH LÊN (không làm yếu đi)** cơ chế (2). Ứng viên **đã đọc nhưng chưa đo**: `cards.js:9 KIND_HOURS`, `cards.js:127 shiftStartMin/shiftEndMin` (client tự đặt neo ca trong khi cadence anchor theo **PHA CA**) — liên quan trực tiếp A2/A11.
10. **CHƯA AI chạy full suite** trong vòng này (`uv run pytest -q` **và** `uv run pytest -q ui/backend/tests`) ⇒ **không phát biểu nào ở đây dựa trên trạng thái suite**. ⚠ Và `ui/backend/tests` đếm **205** trong lần chạy của `rc-L2` trong khi `CLAUDE.md` ghi **201** — **stale lần nữa**, chênh 4 test **chưa ai điều tra**.
11. **Tôi CHƯA kiểm chéo 12 finding đã biết** (`S2-1..S2-6`, `R-1..R-7`) hay các `D-*` được cite — theo yêu cầu, **chỉ CITE**. Nếu một trong số đó sai thì các cycle dựa vào nó (nhất là Cycle 10) đổi.
12. **Câu "kênh TẮT nên vô hại" là ĐÚNG NHƯNG KHÔNG ĐỦ.** Hai đường vòng **đo được**: (a) `demo_session.py:68-71` bật lại `shift_plan`/`accept_lift` cho Track UI (D21); (b) S2 **đã đi dây đầy đủ** trên backend, cổng thật là `missing_state` **chứ không phải ĐA-07** (A12). ⇒ Xếp mọi nợ kênh ngủ vào `DEFERRED` **kèm điều kiện mở lại**, **không** kèm câu *"bán kính = 0"* trần trụi.
13. **Ba hậu quả trong bản đồ này là CẤU TRÚC, chưa reproduce end-to-end:** A4 (ngân sách trọn đời / dismiss vĩnh viễn qua API thật) · D5 (~1.500đ là **DERIVED**) · D24 (tần suất `check_shift_extend` bắn khi `acceptance < 0,85`).
14. **`R-2` — nói cho chính xác, đừng thổi:** tính chất an toàn *"pin thấp thì không hoãn nghỉ"* **VẪN ĐÚNG** (bước 1 của `choose_idle_action` chặn ở thượng nguồn). Cái sai là **BÁO CÁO**: tầng 5 tự trình bày như cổng **3 lan can**, thực tế chỉ **1** (`fatigued`) có thể bắn. **Lỗ hổng HIỂN THỊ, không phải lỗ hổng AN TOÀN.**
15. **`B3` — hồ sơ cũ ghi QUÁ RỘNG.** Sốt ruột có **BỐN** hiệu ứng; **1 trong 4 là no-op** (nới vành 1→3, vì niềm tin chỉ phủ `grid_disk(cell, 2)`), **ba cái kia hoạt động**. Cách phát biểu cũ (*"bước sốt-ruột là NO-OP"*) sẽ **dẫn tới một fix sai chỗ**: sửa vành mà không sửa **bán kính niềm tin** thì vẫn no-op — **hai thứ phải đi cùng nhau**.
16. **Ranh giới đã kiểm:** không đề xuất nào trong bản đồ này đưa **sức khoẻ vào objective**; mọi số tài chính do rule/analytics tính, agent chỉ diễn giải; không khuyên nhận/từ chối đơn cụ thể, không can thiệp matching/dispatch/pricing; **mọi số ở đây là MOCK** (`configs/pilot_dongda.yaml`, dataset `mock-realdata seed_base=7000`) và đã gắn nhãn.
